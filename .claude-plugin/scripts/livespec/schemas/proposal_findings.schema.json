{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "proposal_findings.schema.json",
  "title": "proposal_findings",
  "description": "Schema for the LLM-output JSON payload that propose-change and critique consume — an array of proposal findings, each a candidate proposed-change. Distinct from doctor_findings (the doctor static-phase output). Per PROPOSAL §'doctor → Static-phase orchestrator' line 2624 and §'propose-change' / §'critique'.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "findings"
  ],
  "properties": {
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "target_spec_files",
          "summary",
          "motivation",
          "proposed_changes"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Short proposal name; becomes the heading of the resulting `## Proposal: <name>` section in the proposed-change file."
          },
          "target_spec_files": {
            "type": "array",
            "items": {"type": "string"},
            "description": "Repo-root-relative paths this proposal touches."
          },
          "summary": {
            "type": "string",
            "description": "One paragraph stating what changes and why."
          },
          "motivation": {
            "type": "string",
            "description": "The intent input that produced this proposal."
          },
          "proposed_changes": {
            "type": "string",
            "description": "Prose describing the changes, or a unified diff in fenced ```diff blocks, or both."
          }
        }
      }
    }
  }
}
