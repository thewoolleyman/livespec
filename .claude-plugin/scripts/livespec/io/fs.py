"""Filesystem operations (`@impure_safe`).

Phase 2 placeholder. Real read/write/listdir wrappers land at later
phases; each is `@impure_safe` and returns `IOResult[T, LivespecError]`.
"""

__all__: list[str] = []
