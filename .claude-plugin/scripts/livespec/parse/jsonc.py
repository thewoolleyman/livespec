"""Thin wrapper over vendored jsoncomment (the v026 D1 hand-authored shim).

Phase 2 placeholder. Real `parse(text: str) -> Result[<dataclass>,
PreconditionError]` lands when `livespec_jsonc_valid` and the
`.livespec.jsonc` consumer code paths need it.
"""

__all__: list[str] = []
