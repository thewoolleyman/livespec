# `<intent-derived-title>` — scenarios

This file holds acceptance scenarios in Gherkin form. Each
Gherkin keyword line is preceded and followed by a blank line
so that every step renders as its own Markdown paragraph under
GitHub-Flavored Markdown. Fenced code blocks are not used to
hold Gherkin steps.

## Scenario: `<placeholder-scenario-name>`

Given a placeholder precondition

When a placeholder action is taken

Then a placeholder outcome is observed
