# 02 — Phase 1 dispositions: openbrain

**Repo:** `openbrain` (`/data/projects/openbrain`) — **ADOPTER** (not a fleet member) · **Default branch:** `main` (all evidence read from `origin/main`) · **Ledger:** independent tenant (`ob-*`), via `scripts/with-openbrain-env.sh` · **Phase:** 1 (disposition validation, read-only) · **Date:** 2026-07-03 · **Epic anchor:** `livespec-ztepy5` (LIVESPEC tenant)

**Root-cruft re-check.** `git ls-tree origin/main` confirms exactly two in-scope roots: `research/` and `prompts/` (`PLAN` is out of scope per D3). Full enumeration: `prompts/` contains **only** `AGENTS.md`; `research/` contains exactly 5 files + 1 directory — `android-voice-path-decision.md`, `embedding-model-voyage-investigation.md`, `livespec-impl-probing-proposal.md`, `ob1-fork-patches.md`, `ob1-inline-autonomous-factory.md`, and `gmail-ingest-filter/` (20 files: README, `cache/classifications.json`, 8 `candidates/*.json` + schema, 9 `runs/*.md`). No items missed by the inventory.

## Finalized table

| Item | Final disposition | Evidence |
|---|---|---|
| `research/ob1-fork-patches.md` | **MAINTAINER** (queued for Phase 2 — not resolved here) | All inventory refs verified on `origin/main`: `lefthook.yml:133` (inert-deferral-lint glob), `AGENTS.md:214`, `SPECIFICATION/spec.md:1667`, `SPECIFICATION/scenarios.md:1772` (third-fork-patch `id: number→string` scenario), `.ai-instructions/local-gates.md:119`, `.ai-instructions/ob1-fork.md:61`, `.claude/workflows/implement-task-capture-surfaces.mjs:218` (full path resolved), `README.md:147`. ADDITIONAL refs found: `scripts/inert-deferral-lint.ts:47` (header) and `:84` (allowlist regex `/^research\/ob1-fork-patches\.md$/`), `README.md:376` (layout-tree row). Registry is live and append-only: last append **Patch 20, 2026-06-21** (commit `7b9f776`); 20 patches total. |
| `research/android-voice-path-decision.md` | **SPEC-ABSORB** (confirmed) | Spec anchor holds: `SPECIFICATION/spec.md:1768` cites it for the settled App-Actions-spike outcome. **Characterization: content still TRUE of deployed reality** — the widget voice path is implemented (`VoiceCaptureActivity.kt`, `AndroidManifest.xml` wiring) and the load-bearing rationale is ALREADY inline in spec prose ("the widget shape is the settled outcome of the App-Actions spike … assistant-routed BII fulfillment is unavailable upstream"). The propose-change is therefore a light de-reference: keep the inline rationale clause, repoint the link to `archive/research/…` (or drop it) — nothing new to absorb. Last commit 2026-06-10. |
| `research/gmail-ingest-filter/` | **SPEC-ABSORB** (confirmed; tool-coupling sub-question escalated) | Spec anchor holds: `SPECIFICATION/contracts.md:663` links `candidates/human-conversations-stricter.json` as the value the seed was "lifted verbatim" from. **Characterization: the link is provenance-only and STALE as a deployed-definition pointer** — the linked candidates file is frozen at `"version": 3` (still denylists `noreply`/`linkedin`; empty `include_senders`; no `gtd_status_label_map`/`after_relative_days`) while contracts.md's embedded normative seed value is **`"version": 8`** (v4 gtd map 2026-05-24; v5 date window 2026-06-10 li-3jlxax; v6 drops the no-reply/linkedin denylist + adds the LinkedIn-digest allowlist; v7 label-map enforcement; v8 GTD-redesign type retirement). The spec already carries the authoritative value fully inline; the propose-change should rewrite the provenance clause as history (archive path or no link), NOT inline anything. Deployed reality is the live JSONB row seeded/updated by migrations `20260504082752_seed_gmail_ingest_filter.sql` (comment cites the candidates JSON, line 11), `20260510100000_gmail_ingest_filter_v3_exclude_recipients.sql` (cites it, line 10), `20260611210000_…_v6_drop_noreply_linkedin_allowlist.sql`, `20260611210500_…_v6_fix_guard.sql`, `20260612200000_…_v7_label_map_enforced.sql` (v4/v5/v8 were operator JSONB edits per the contract's own rule). Directory is ALSO functionally coupled to a live tool — see Escalation 2. Last commit 2026-06-23. |
| `research/embedding-model-voyage-investigation.md` | **ARCHIVE** (verified) | Zero current-file inbound refs (full sweep). Landing evidence: `SPECIFICATION/history/v043/proposed_changes/embedding-pipeline-chunking.md` accepted the chunking change with the model **explicitly UNCHANGED** (`openai/text-embedding-3-small`, `vector(1536)`) and the Voyage question tracked separately in **ob-nwf**; ledger `ob-nwf` is **CLOSED** (updated 2026-06-23, under epic li-rm3ss2) with a concluded verdict: "DO NOT switch … Voyage not on OpenRouter; quality delta within noise; quality optimization not a recall fix", naming this doc as the landed deliverable. The doc self-describes as "investigation deliverable — a recommendation, not a change" with explicit revisit triggers, which the archive copy preserves. Last commit 2026-06-12 (`ed3a365`). |
| `research/livespec-impl-probing-proposal.md` | **ARCHIVE** (verified) | Zero current-file inbound refs (full sweep). Superseded by adopter registration on BOTH sides: livespec `origin/master:.livespec-fleet-manifest.jsonc` carries the `adopters` entry "openbrain: first registered adopter (github-app-auth D17, 2026-07-03)" (profile `["baseline","orchestrator-plugin"]`, posture `pinned`); openbrain-side 2026-07-03 merges `f72796d` (declare `credential_wrapper` in `.livespec.jsonc`), `4b2456a` (adopter-adapted fabro implement-work-item workflow), `113a7a6` (spec **v091** codifies the github-app-auth adopter-dispatch contract). Last commit to the doc 2026-06-23 (`948ded9`). |
| `research/ob1-inline-autonomous-factory.md` | **STAYS** (corrected from ARCHIVE — live code ref) | Inventory's "none found" is WRONG: `scripts/promote.ts:6` (header of the live promotion state machine, wired as `package.json:39` `promote:dashboard`) cites the doc as "the openbrain-local promotion driver chosen by research/ob1-inline-autonomous-factory.md". Per confirmed rule D1 (docs referenced only by code stay in `research/`) the disposition is STAYS. Landing evidence is nonetheless solid: `SPECIFICATION/history/v086/proposed_changes/ob1-inline-autonomous-deploy.md` accepted; epic `ob-coq` **CLOSED** 2026-06-23 ("accepted v086 spec … added promote:dashboard state machine, wired Honeycomb gates"); archived handoff present at `archive/prompts/ob-coq-handoff-ob1-inline-autonomous-factory.md`. Last commit 2026-06-23. |
| `prompts/AGENTS.md` | **D2: fold → archive → remove dir** (confirmed) | Only tracked file in `prompts/` on `origin/main` (verified by full `ls-tree`). Content to fold into `AGENTS.md`: its four sections — `## Naming convention`, `## Lifecycle`, `## Directives for every session run from a handoff prompt`, `## Authoring checklist`. Inbound refs verified: `AGENTS.md:31` (directory-map row), `AGENTS.md:345`, `AGENTS.md:349`, `README.md:283`. `README.md:283` is confirmed ALREADY stale — it cites "handoffs under prompts/ (li-ruxxwt, li-agfejw)" while both live at `archive/prompts/li-ruxxwt-handoff-meta-orchestrator.md` and `archive/prompts/li-agfejw-handoff-gmail-triage-gating.md`. Livespec-side compatibility: livespec `non-functional-requirements.md` §"No shadow ledger" requires "a per-repo `prompts/AGENTS.md` **(or equivalent)**" — the folded `AGENTS.md` section IS the equivalent; no cross-repo blocker. Last commit 2026-06-15. |

## Corrections vs inventory

1. **`ob1-inline-autonomous-factory.md`: ARCHIVE → STAYS.** The inventory missed the live code ref `scripts/promote.ts:6`. Rule D1 applied mechanically flips it to STAYS. (If Phase 2 prefers pruning landed design records from `research/`, the cost is a single comment retarget at `scripts/promote.ts:6` — noted, not recommended, since the promotion driver is active machinery and the doc is its named design record.)
2. **`android-voice-path-decision.md`: two missed refs.** `android-capture/README.md:18` and `android-capture/README.md:159` also link the doc — five total inbound refs, not three.
3. **`gmail-ingest-filter/`: missed live tool coupling.** `scripts/explore-gmail.ts` hard-codes `RESEARCH_DIR = research/gmail-ingest-filter` (line 34) and reads/writes `candidates/`, `runs/`, `cache/` (lines 12–13, 16, 25, 197, 208, 214–215); wired as `package.json:51` (`explore:gmail`) with sibling `package.json:52` (`test:explore-gmail-classifier`); documented at `scripts/README.md:158`. Also missed: `README.md:377` layout-tree row. Migration evidence is five files, not two (the two comment-citing ones plus the v6×2 and v7 migrations).
4. **`ob1-fork-patches.md`: ref list adjustments.** The ".mjs:218" ref resolves to `.claude/workflows/implement-task-capture-surfaces.mjs:218`; two additional code refs exist in `scripts/inert-deferral-lint.ts` (`:47` header, `:84` allowlist regex), plus the `README.md:376` layout row. All originally-listed refs verified intact.
5. **Sweep false-positives / out-of-repo hits (for the record):** `.fabro/workflows/implement-work-item/workflow.fabro:225,259,289` reference the workflow-LOCAL `.fabro/workflows/implement-work-item/prompts/` dir (exists on `origin/main`) — not the root `prompts/`; `AGENTS.md:304` references **livespec's** `research/beads/beads-gaps-workarounds.md` (cross-repo — see Escalation 3); `README.md:375` is the `research/` layout-tree row itself.

## Escalations for Phase 2

1. **`ob1-fork-patches.md`** — Recommended verdict: **exempt-and-stay** — a live append-only registry (Patch 20, 2026-06-21) with 12 inbound refs including an enforcement-gate glob (`lefthook.yml:133`) and a lint allowlist regex (`inert-deferral-lint.ts:84`); absorbing mutable tracking state into the spec violates "spec is for contracts, not tracking", and relocation churns 12 refs plus gate config for no governance gain.
2. **`explore-gmail.ts` tool coupling (sub-question of the gmail-ingest-filter SPEC-ABSORB)** — Recommended verdict: **retire the tool alongside the directory archive** (archive `scripts/explore-gmail.ts` + `scripts/test-explore-gmail-classifier.ts`, drop `package.json:51–52` scripts, drop `scripts/README.md:158` row) — the Phase-0 filter question is settled, live filter edits are operator JSONB edits per contracts.md, and the rejected alternative (retargeting `RESEARCH_DIR` into `archive/research/`) would have live tooling writing into the frozen archive.
3. **Cross-repo ref: `AGENTS.md:304` → livespec `research/beads/beads-gaps-workarounds.md`** — Recommended verdict: **hand to the livespec-repo disposition track** — if livespec's cleanup moves that doc, openbrain `AGENTS.md:304` needs a same-epic retarget; nothing to do openbrain-side otherwise.

## Reference-update obligations (Phase 3 executor)

**Gate (must be green for every Phase 3 commit).** Lefthook is the enforcement surface (the `just check` skeleton added 2026-07-03 in `3d3a784` is additive-only until epic ob-23p's migrate slice): pre-commit — `refuse-primary-commit`, `validate-plan-schemas`, `tsc-no-emit-{scripts,tsconfig,package}`, `dashboard-tsc-no-emit`, `dashboard-lint`, `deno-check`, `secrets-guard`, `deploy-wrapper-guard`, `migration-filename`, `inert-deferral-lint`, `symlink-integrity-{claude,agents}`; commit-msg — `refuse-primary-commit`, `inert-deferral-lint-msg`; pre-push — `release-prod-dry-run`, `release-staging-dry-run`, `verify-openbrain-env`, `wrappers-table-drift`, `dashboard-build`, `release-android`. Note `inert-deferral-lint` globs `research/ob1-fork-patches.md` and `supabase/migrations/*` — any Phase-2-decided move of the registry MUST co-edit `lefthook.yml:133` and `scripts/inert-deferral-lint.ts:47,84`.

**Ordering rule for both SPEC-ABSORB rows (strict):** file the propose-change through openbrain's own livespec cycle → accept via `/livespec:revise` (a new `SPECIFICATION/history/vNNN/` records the de-reference) → **only then** `git mv` the doc/directory to `archive/research/` with the non-spec ref retargets in the same change. Never move first.

**D2 (prompts/ retirement) — exact edits:**
- `AGENTS.md:31` — rewrite the `prompts/` directory-map row: convention folded into AGENTS.md; active handoffs live in `plan/<topic>/handoff.md`; historical handoffs in `archive/prompts/`.
- `AGENTS.md:345` and `AGENTS.md:349` — repoint the handoff-convention pointers at the folded-in section.
- `README.md:283` — fix NOW regardless of D2 sequencing (already stale): repoint `prompts/ (li-ruxxwt, li-agfejw)` to `archive/prompts/li-ruxxwt-handoff-meta-orchestrator.md` + `archive/prompts/li-agfejw-handoff-gmail-triage-gating.md`.
- Fold `prompts/AGENTS.md` §§ Naming convention / Lifecycle / Directives / Authoring checklist into `AGENTS.md`, then `git mv prompts/AGENTS.md archive/prompts/AGENTS.md` and remove the empty dir.

**SPEC-ABSORB: android-voice-path-decision.md** (revise first, then move to `archive/research/android-voice-path-decision.md`):
- `SPECIFICATION/spec.md:1768` — via the revise payload: keep the inline rationale clause, repoint or drop the link.
- `android-capture/app/src/main/AndroidManifest.xml:35` — comment path → archive path.
- `android-capture/app/src/main/kotlin/com/openbrain/capture/VoiceCaptureActivity.kt:14` — comment path → archive path.
- `android-capture/README.md:18` and `android-capture/README.md:159` — link paths → archive path.

**SPEC-ABSORB: gmail-ingest-filter/** (revise first, then move the directory to `archive/research/gmail-ingest-filter/`; tool verdict per Escalation 2):
- `SPECIFICATION/contracts.md:663` — via the revise payload: rewrite the "lifted verbatim" provenance clause as history (the spec's v8 value is already authoritative inline; the candidates file is frozen at v3).
- `scripts/explore-gmail.ts:12,13,16,25,34,197,208,214,215` + `package.json:51,52` + `scripts/README.md:158` — per the Phase 2 tool verdict (retire recommended; retarget otherwise).
- `supabase/migrations/20260504082752_seed_gmail_ingest_filter.sql:11` and `supabase/migrations/20260510100000_gmail_ingest_filter_v3_exclude_recipients.sql:10` — **DO NOT EDIT** (applied migrations are immutable; the path citations are frozen historical provenance).
- `README.md:377` — layout-tree row for `gmail-ingest-filter/` → remove or repoint.

**README layout tree (ride-along with the last Phase 3 change):** `README.md:375` (`research/` described as "active exploratory docs") — reword to reflect the post-cleanup residents (per Phase 2: likely `ob1-fork-patches.md` + `ob1-inline-autonomous-factory.md` only); `README.md:376` (`ob1-fork-patches.md` row) — per Phase 2 verdict.

**No-obligation rows:** `embedding-model-voyage-investigation.md` and `livespec-impl-probing-proposal.md` move to `archive/research/` with zero ref edits (ledger `ob-nwf`'s close-reason path citation is an immutable historical record — leave it); `ob1-inline-autonomous-factory.md` STAYS — `scripts/promote.ts:6` remains valid as-is.

## Phase 2 verdicts — CONFIRMED by the maintainer (2026-07-03)

| Escalation | CONFIRMED verdict |
|---|---|
| 1 — `ob1-fork-patches.md` | **Exempt-and-stay** — recorded D1 exemption: a live append-only registry is TRACKING STATE the spec may reference without absorbing ("spec is for contracts, not tracking"); zero moves, zero ref churn. The exemption is documented here; Phase 5's re-scan treats this spec-referenced research file as deliberate. |
| 2 — `explore-gmail.ts` tool coupling | **Retire the tool with the directory** — archive `scripts/explore-gmail.ts` + `scripts/test-explore-gmail-classifier.ts` (→ `archive/research/gmail-ingest-filter/tools/` or beside the archived dir), drop `package.json:51–52` script entries and the `scripts/README.md:158` row, in the same PR as the directory archive (after the de-reference revise lands). |
| 3 — cross-repo `AGENTS.md:304` → livespec beads-gaps-workarounds | No action — the livespec target STAYS; optional ride-along: qualify the reference as livespec-repo-explicit. |
