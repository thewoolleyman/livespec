# Livespec is the source of the copier template at templates/orchestrator-plugin/;
# drift detection does not apply to the source itself. This file exists
# to satisfy the `copier-template-workflow-coverage` doctor invariant per
# SPECIFICATION/contracts.md §"copier-template-workflow-coverage", which
# requires every required-list workflow to be present in every
# livespec-governed repository (including livespec itself).

name: copier-update-drift

on:
  workflow_dispatch:

jobs:
  noop:
    name: N/A (livespec is the template source)
    runs-on: ubuntu-latest
    steps:
      - name: Explain
        run: |
          echo "livespec hosts templates/orchestrator-plugin/ — the copier template source."
          echo "copier update --dry-run does not apply to the source repo itself."
          echo "This workflow exists to satisfy copier-template-workflow-coverage."
