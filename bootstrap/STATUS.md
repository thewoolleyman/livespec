# Bootstrap status

**Current phase:** 8
**Current sub-step:** Phase 8 sub-step 2. Item 2 — `python-style-doc-into-constraints` (per-section migration of `brainstorming/approach-2-nlspec-based/python-skill-script-style-requirements.md` into `SPECIFICATION/constraints.md` / `SPECIFICATION/spec.md`). Source doc has 23 top-level `##` sections (lines 43-2211); each section is its own propose-change/revise cycle per Plan Phase 8 item 2's per-section split rationale. **Cycles 1-4 complete (v016/v017/v018/v019 cut).** 19 cycles remaining. The first cycle's revise body acknowledged the deviation from `deferred-items.md` §`python-style-doc-into-constraints`'s "at seed time" guidance (per Phase 6's audit-granularity reason); subsequent cycles cross-reference cycle 1.
**Last completed exit criterion:** phase 7
**Next action:** Cycle 5 = §"Package layout" (style doc lines 300-514, ~215 lines — large section, no existing constraints.md analog). Recommended approach: NEW `## Package layout` section in SPECIFICATION/constraints.md (or possibly SPECIFICATION/spec.md depending on heading-taxonomy fit; package layout is partly architectural-constraint and partly descriptive-structure — read source-doc content first to choose). Author propose-change `python-style-package-layout` against SPECIFICATION/; revise → cuts SPECIFICATION/v020. Cross-reference cycle 1's revise body for deviation rationale. After cycle 5: cycle 6 = §"Railway-Oriented Programming (ROP)" (lines 516-643, ~128 lines, MERGE with existing `## ROP composition`). Subsequent cycles 7-23 continue per source-doc reading order: §"Sub-command lifecycle composition" (likely SPECIFICATION/spec.md merge with §"Sub-command lifecycle"), §"Purity and I/O isolation" (merge with §"Pure / IO boundary"), §"Type safety" (264 lines, merge with §"Typechecker constraints"), §"Linter and formatter", §"Testing" (merge with spec.md §"Testing approach"), §"Test-Driven Development discipline", §"Code coverage", §"Keyword-only arguments", §"Structural pattern matching", §"Complexity thresholds" (merge with §"File LLOC ceiling"), §"Structured logging", §"Exit code contract", §"File naming and invocation", §"Dev tooling and task runner" (likely spec.md §"Developer-tooling layout"), §"Enforcement suite", §"CLAUDE.md coverage", §"Non-goals" (likely spec.md §"Non-goals" merge).

**Phase 8 progress summary (2026-05-06T04:32:25Z → 2026-05-06T04:53:47Z; 14 commits across 5 cycles — item 1 + item 2 cycles 1-4):**

**Item 1 (`template-prompt-authoring` closure)** — bookkeeping closure for deferred-item closed in v018 Q1; mechanism shipped in Phase 6 multi-tree seed + Phase 7 sub-spec authoring cycles ((c).1-(c).6 livespec, (d).1-(d).6 minimal). Two commits: `8bd8c03` filed propose-change with closure rationale + PROPOSAL/Phase-7 anchor pointers; `94ce632` invoked `bin/revise.py --revise-json` with single `reject` decision per v038 D1 Statement B audit-trail-preservation contract → cut SPECIFICATION/v015 byte-identically. Original propose-change moved into `history/v015/proposed_changes/`; auto-generated revision file carries §"Decision and Rationale" + §"Rejection Notes". STATUS rollup at `9a63e15`.

**Item 2 (`python-style-doc-into-constraints` per-section migration)** — cycles 1-3 of 23 complete. Pattern per cycle: (a) propose-change commit author the topic file under `SPECIFICATION/proposed_changes/python-style-<section-slug>.md` with BCP-14-restructured target content; (b) build `tmp/bootstrap/build_phase_8_item_2_cycle_N_revise.py` helper composing `replace_once`-anchored payload; (c) invoke revise wrapper; (d) update `tests/heading-coverage.json` for any newly-introduced `## ...` headings (TODO entries with section-specific reason); (e) commit revise output bundling v0NN/ snapshot + spec edit + heading-coverage update.

- **Cycle 1 (`3f99c47` → `23771f3`, v016):** §"Scope" (style doc lines 43-71). Inserted new `## Constraint scope` section after intro paragraph + before `## Python runtime constraint`; also rewrote intro paragraph from Phase-6 deferral note to Phase-8-in-progress note. Cycle 1's revise body acknowledged the deviation from `deferred-items.md` "at seed time" guidance per Phase 6's audit-granularity rationale; subsequent cycles cross-reference cycle 1.
- **Cycle 2 (`b3f9cc6` → `e54f0c4`, v017):** §"Non-interactive execution" (lines 73-87). Inserted new `## Non-interactive execution` section after `## Constraint scope`. Already-BCP-14-shaped source content; no semantic restructure required.
- **Cycle 3 (`235ce58` → `7db5ea2`, v018):** §"Interpreter and Python version" (lines 91-110). MERGED into existing `## Python runtime constraint` (replaced 1-paragraph body with 4-paragraph body incorporating every source-doc bullet — Python 3.10+ idioms, 3.11+ ban, shebang, `_bootstrap.py` version-check, `.python-version` + `.mise.toml` + `uv sync` tooling chain). Heading retained — merge preferred over sibling-insert; no heading-coverage update required.
- **Cycle 4 (`adb56f3` → `fdcf830`, v019):** §"Runtime dependencies" (lines 114-298, ~184 lines, source-doc 3 `###` sub-sections). Split into TWO destination sections: NEW `## End-user runtime dependencies` (from ### End-user install, inserted between `## Python runtime constraint` and `## Vendored-library discipline`) + EXPANDED `## Vendored-library discipline` (existing heading retained, body replaced with merged content using `### Lib admission policy` + `### Locked vendored libs` + `### Vendoring procedure` sub-headings). Lib enumeration retained for all 4 vendored libs (returns / fastjsonschema / structlog / jsoncomment / typing_extensions); historical context (v018 Q4 / v025 D1 / v026 D1 / v027 D1) cross-referenced via "see PROPOSAL.md §..." pointers rather than fully re-stated. New `## End-user runtime dependencies` heading registered in tests/heading-coverage.json.

Cascading-impact scans: skipped per "purely mechanical migration with no new convention introduced" — every cycle is a verbatim Plan Phase 8 item 2 prescription transcription with BCP-14 wording polish.

**Sub-step 6.a + 6.b summary (2026-05-04T23:25:00Z → 2026-05-05T00:27:00Z):**

Sub-step 6.a authored `SPECIFICATION/proposed_changes/prune-history-full-feature-parity.md` (committed at `56fc697`) with three proposals scoped to PROPOSAL-authorized parity (no contract extensions): (P1) CLI surface row replacement; (P2) lifecycle and responsibility separation in spec.md §"Sub-command lifecycle" (5-step prune mechanic + invariants + no-op short-circuits); (P3) Pre-step skip control mechanism shared by every pre-step-having wrapper. Cascading-impact scan during authoring caught a pre-existing drift between PROPOSAL.md §"Spec-target selection contract" line 363-366 (3 wrappers in --spec-target list, no doctor) and seeded SPECIFICATION/contracts.md line 50 (4 sub-commands, includes doctor); recorded in `bootstrap/decisions.md` at 2026-05-04T23:30:00Z as a side observation per "one investigation, one finding, one question" memory rule — not addressed in 6.a.

Sub-step 6.b invoked the now-widened `bin/revise.py --revise-json <payload> --spec-target SPECIFICATION/` via the throwaway `tmp/bootstrap/sub-step-6b-helper.py` (computed cumulative resulting_files content, built revise_input.schema.json-conforming payload, dispatched). Single accept-decision aggregating all three proposals' edits per the 5.b precedent (one decision per topic file). The revise wrapper exercised all four 5.c-cycle behaviors in production: (a) full 5-key front-matter + `## Decision and Rationale` + `## Resulting Changes` revision-md auto-generated; (b) per-decision-type sections; (c) working-spec snapshot to `<spec-root>/history/v012/` (constraints.md, contracts.md, README.md, scenarios.md, post-edit spec.md all snapshotted byte-identically); (d) successful revise even with non-empty proposed_changes/ (precondition check passes). v012 cut unconditionally per v038 D1 Statement B. Original `proposed_changes/prune-history-full-feature-parity.md` moved byte-identically into `history/v012/proposed_changes/`. End-to-end dogfooding confirmed the widened revise contract works.

**v039 codification summary (2026-05-04T08:30:00Z):**

User-initiated perf + discipline cycle interrupting sub-step 5.c after the prior session timed out on a Green-amend coverage-gap loop. Six decisions: D1 drop `check-tests` from canonical aggregate (~108s saved); D2 add `pytest -n auto` to `check-coverage` (~150s saved on multi-core); D3 introduce `just check-coverage-incremental` path-scoped fast-feedback target; D4 codify Red-time branch enumeration + proactive coverage discipline as PROPOSAL.md §"Test-Driven Development discipline" sub-section; D5 defer pytest-cov subprocess-instrumentation path-translation spike to highest-priority follow-up after wip/comment-line-anchors lands; D6 plan-text + housekeeping (Phase 0 byte-identity bump → history/v039/, frozen-status header → "Frozen at v039", execution-prompt authoritative-version → v039). Bundle rationale: shared iteration-loop thesis (perf-without-discipline leaves loop slow on missed branches; discipline-without-perf has no fast-feedback layer). Item 3 from the original perf design (collapse 29 AST checks into one runner) was rejected during the design discussion — saves ~12s but collides with v033 D1 mirror-pairing + v033 D2 per-file coverage + main-guard + wrapper-shape invariants. v039 codification commit at `fa7f3bd`.

**v038 halt-and-revise summary (2026-05-04T03:43:15Z → 2026-05-04T06:55:00Z):**

Phase 7 sub-step 5.a's cascading-impact scan caught a PROPOSAL.md §"Versioning" internal contradiction between Statement A (line 1734-1736 iff: cut "when, and only when, accepts or modifies") and Statement B (line 1739-1745: all-reject MUST still cut). User selected Statement B as authoritative. v038 D1 PROPOSAL edits at two loci; line 1739-1745 case-handler retained verbatim. v038 D2 plan-text + housekeeping. v038 codification commit at `1280ae6`.

**Sub-step 5.c implementation summary (2026-05-04 → 2026-05-05, four Red→Green cycles per the v039 D3 incremental + v033 D5b mirror-pairing + v034 D2-D3 trailers contract):**

- **Cycle 1 (`9547caa` — feat: red — io.git.get_git_user resolves git user.name + user.email):** Drives the `livespec/io/git.py` module into existence per the seam named at PROPOSAL.md §"Revision file format" line 3123-3124 + `revision_front_matter.schema.json`'s `author_human` description. The widened `revise.py` consumes this seam to populate `author_human`.

- **Cycle 2 (`91a076e` — feat: red — revise emits 5-key front-matter + per-decision-type sections):** Widens `livespec/commands/revise.py`'s `_compose_revision_body` from the Phase-3-minimum 2-key shape (proposal + decision; only `## Decision and Rationale` body) to the v011-spec-codified full shape: 5-key front-matter (proposal, decision, revised_at, author_human, author_llm) + per-decision-type body sections (`## Decision and Rationale` always; `## Modifications` when modify; `## Resulting Changes` when accept|modify; `## Rejection Notes` when reject). Eight new tests across the front-matter + section coverage matrix.

- **Cycle 3 (`8d08cfb` — feat: red — revise snapshots spec files into history/vNNN/):** Implements v011 Proposal 3 item d / v038 D1 Statement B's "version cut on every successful revise" contract via a new `_snapshot_working_spec_files` helper threaded into `_process_decisions` after the per-decision write-and-move chain. Snapshot rule: every immediate file child of `<spec-target>/` is byte-identically copied into `<spec-target>/history/vNNN/`; subdirectory children (`history/`, `proposed_changes/`, `templates/`) are skipped (not template-declared spec files). For accept/modify decisions, the snapshot reflects post-`resulting_files` content (sequencing is resulting-files-write -> snapshot). Two new tests pin both branches.

- **Cycle 4 (`7a1bd41` — feat: red — revise fails fast on empty proposed_changes/ with PreconditionError):** Implements v011 Proposal 3 item a: revise fails hard with `PreconditionError` (exit 3) when `<spec-target>/proposed_changes/` contains no in-flight proposal files (the skill-owned `proposed_changes/README.md` does not count). The check fires at the railway boundary BEFORE any `history/vNNN/` materialization, so a precondition fail leaves the spec tree byte-identical. Two new tests pin both the README-only-present and completely-empty branches. Per-cycle bookkeeping: the Green amend forced `revise.py` over the 250-LLOC hard ceiling, so the impure railway-emission helpers (`_process_decisions`, `_check_proposed_changes_nonempty`, `_verify_in_flight_proposals_present`, `_next_history_version_dir`, `_format_next_version_name`, `_write_and_move_per_decision`, `_bind_resulting_files`, `_snapshot_working_spec_files`, `_copy_files_into`) were extracted to a new sibling private module `livespec/commands/_revise_railway_emits.py` (matching the established `_seed_railway_emits.py` precedent). Re-exports in `revise.py` preserve the test surface.

**Sub-step 5 commit chain (oldest→newest):** `1280ae6` (v038 codification) → `9eca318` (5.a) → `6646aa4` (5.b) → `14113ba` (interim STATUS) → `9547caa` (5.c cycle 1) → `91a076e` (5.c cycle 2) → `f3d2380` (line-number anchor scrub) → `fa7f3bd` (v039 codification) → `0f1ad38` (interim STATUS) → `2edda62` (v039 D2 — pytest-xdist) → `2aeeccc` (v039 D1 — drop check-tests) → `81d5baa` (v039 D3 — check_coverage_incremental) → `61410c2` (comment_line_anchors check) → `6c82d85` (interim STATUS) → `a622381` (fix: _write_trailers replace existing keys) → `aaaaa82` (refactor: cache proposal_findings schema at module level) → `7c39a79` (v040 codification) → `693e980` (interim STATUS) → `6028ba2` (v040 D1 enforcement) → `6b2e657` (interim STATUS) → `8d08cfb` (5.c cycle 3) → `7a1bd41` (5.c cycle 4 + helper extraction).

**v040 codification summary (2026-05-04 → 2026-05-05):**

`chore!:` codification of "flaky tests are unacceptable" as a PROPOSAL.md hard constraint per user direction; new sub-section under §"Test-Driven Development discipline" (commit `7c39a79`). v040 D1 enforcement via `@settings(deadline=None)` + module-level `_SCHEMA` cache uniformly applied across all 10 validate/ hypothesis test files (commit `6028ba2`). Verified by 5 consecutive validate-only runs + 3 consecutive full check-coverage runs all green pre-cycle-3. Full `just check-coverage` aggregate post-v040: 459 tests pass (after 5.c cycle 3+4 added 4 more), 100% line+branch coverage, ~28 seconds wall-clock (down from ~5:49 pre-v039 per the v039 D2 wall-clock target).

**Sub-step 4.c implementation summary (2026-05-04, preserved from prior STATUS):**

Single Red→Green cycle landed in two commits (Red `ad6ed2e` superseded by Green amend at `de1c110`): `_resolve_author` widening to full 4-step precedence + reserve-suffix-parameter delegation. v034 D2-D3 amend hiccup (codified): the first impl draft passed `--reserve-suffix` and `-critique` as two separate argv tokens — argparse rejected this; fix: use the `--reserve-suffix=-critique` single-arg `=` syntax. Lesson: when forwarding flag values that begin with `-`, always use the `=` form.

**Phase-7 architectural follow-up logged in `decisions.md` 2026-05-03T10:50:00Z:** `heading_coverage.py` treats `## Proposal:` headings under `proposed_changes/` as spec content; PROPOSAL.md §"Coverage registry" lines 3795-3798 narrowly define `spec_root` as tree-root only. Pre-existing PROPOSAL drift accepted at Phase 6; deferred to Phase 7 propose-change cycle or Phase 8 deferred-items entry. Not blocking.

**Sub-step 3.c implementation summary (preserved from prior session):**
- Cycle 1 — topic canonicalization (v015 O3): added `_canonical_alnum_run_strip(*, text)` shared primitive + `_canonicalize_topic(*, hint, reserve_suffix=None)`.
- Cycle 2 — reserve-suffix (v016 P3 / v017 Q1): widened `_canonicalize_topic` with `reserve_suffix` keyword.
- Cycle 3 — author resolution + YAML front-matter: added `_resolve_author(*, namespace, payload, env_lookup)` implementing 4-step precedence in propose_change.py.
- Cycle 4 — slug transformation (v014 N5): structural no-op (decisions.md 2026-05-04T02:10:00Z).
- Cycle 5 — collision disambiguation (v014 N6): added `_resolve_target_path(*, proposed_changes_dir, canonical_topic, existing_filenames)`.

**v034 scope (eight decisions):** D1 Conventional Commits + semantic-release adoption ✓; D2 TDD-Red/Green trailer schema ✓; D3 replay-based enforcement contract via `dev-tooling/checks/red_green_replay.py` ✓; D4 `refs/notes/commits` as advisory operational cache ✓; D5 plan-text + dev-tooling enumeration housekeeping ✓; D6 baseline-grandfathered-violations TOML mechanism (deferred indefinitely per v035 D1); D7 Phase 5 §"Aggregate-restoration drain" ✓; D8 branch protection on master ✓.

**Pre-v034 cycle history preserved as-is:** v033 D5b second-redo cycles 1-172 used the v033 discipline (`## Red output` honor system; `phase-N: cycle N — ...` commit prefix). Grandfathered: commitlint excludes pre-v034-codification ancestor SHAs.

**Sub-step (f) (prompt-QA harness machinery) summary (2026-05-05T21:54:00Z → 2026-05-06T00:35:00Z):**

Phase 7 sub-step (f) closed in 12 effective commits across two turns: 3 commits for the spec-change cycle (`554397a` propose-change + `6b84b75` revise → SPECIFICATION/v014 + `c223b1a` heading-coverage fix-forward), 1 STATUS rollup (`36712fd`), and 8 implementation cycles (`9679623` foundational scaffold + livespec seed baseline + 8 harness unit tests; `45882ec`/`b9c7e10`/`927e900` livespec propose-change/revise/critique baselines; `4d5848c` minimal scaffold + seed baseline; `c81cf69`/`1467dee`/`ef68992` minimal propose-change/revise/critique baselines). End state: `tests/prompts/_harness.py` + `_fixture.schema.json` + per-template `_assertions.py` registries (empty until Phase 7 items (c)/(d) widen them) + 8 prompt-QA fixtures (4 prompts × 2 templates = the v018 Q5 minimum) + 8 harness unit tests covering every branch of the harness module's behavior contract. `just check-prompts` runs 16/16 green. PROPOSAL drift on `[tool.coverage.run].source` discovered during cycle (f).2 (PROPOSAL claims source-allowlist; pyproject.toml uses omit-only); harness module added to coverage measurement and brought to 100% via the 8 unit tests as a tactical fix-forward. The drift is logged for halt-and-revise reconciliation when convenient.

**Sub-step (f).1 (spec-change cycle) summary (2026-05-05T21:54:00Z → 2026-05-06T00:05:00Z):**

Phase 7 sub-step (f) prompt-QA harness machinery's spec-change cycle landed in three commits per the established 6.a/6.b precedent (propose-change → revise → fix-forward). Sub-step (f).1.A authored `SPECIFICATION/proposed_changes/prompt-qa-harness-machinery.md` (committed at `554397a`) with two proposals scoped to PROPOSAL-authorized prompt-QA tier codification (no contract extensions): (P1) test-pyramid widening in spec.md §"Testing approach" acknowledging the prompt-QA tier above the unit-coverage layer (scope-distinct from the v014 N9 e2e tier; non-coverage-measured because tests/prompts/ is outside the unit-tier coverage source list); (P2) prompt-QA harness contract appended to contracts.md as a new top-level §"Prompt-QA harness contract" section codifying harness location at `tests/prompts/_harness.py` (dedicated test-infrastructure module, NOT a stripped-down `tests/e2e/fake_claude.py` variant — the two harnesses are scope-distinct), fixture format at `tests/prompts/_fixture.schema.json` with six required fields (`prompt_name`, `schema_id`, `input_context`, `replayed_response`, `expected_schema_valid`, `expected_semantic_properties`; the latter four are explicit in deferred-items.md `prompt-qa-harness`, the first two are added to give the harness explicit dispatch keys), per-template assertion registry at `tests/prompts/<template>/_assertions.py` exporting `ASSERTIONS: dict[str, Callable[..., None]]` via static enumeration with explicit imports (no `glob+importlib` dynamic discovery — pyright strict can verify completeness), 4-step harness behavior (load fixture → schema-validate fixture-format → schema-validate replayed_response → dispatch per-name assertions), and full Python-rule compliance (`__all__` + keyword-only args + return types + private-helper underscore prefix). Sub-step (f).1.B invoked `bin/revise.py --revise-json --spec-target SPECIFICATION/` via the throwaway `tmp/bootstrap/build_prompt_qa_revise_json.py` helper (computed cumulative resulting_files for spec.md + contracts.md, built revise_input.schema.json-conforming payload, dispatched). Single accept-decision aggregating both proposals' edits per the v011 5.b precedent (one decision per topic file). Cut SPECIFICATION/history/v014/ unconditionally per v038 D1 Statement B with byte-identical snapshots of all five top-level spec files; auto-generated revision file at `history/v014/proposed_changes/prompt-qa-harness-machinery-revision.md`; original propose-change file moved alongside it. Committed at `6b84b75`. Sub-step (f).1.C fix-forward registered the new `## Prompt-QA harness contract` heading in `tests/heading-coverage.json` as a TODO entry per the established sub-step 3.b "## Author identifier resolution" pattern (line 577); committed at `c223b1a`. Cascading-impact scan at each step against PROPOSAL.md + bootstrap plan was clean.

**Last updated:** 2026-05-06T14:30:00Z
**Last commit:** `071bfe1` on phase-8-python-style-migration (branch cut from master HEAD; prior Phase 8 cycles 1-4 merged via PR #4 `phase-7-widen-sub-commands`; chore PRs #5/#6/#7 also merged; fresh phase-8-python-style-migration branch cut per Guard Layer 3 step 3). Cycle pattern firm: propose-change commit → revise commit (bundles v0NN/ snapshot + spec edit + heading-coverage update if new headings introduced).
