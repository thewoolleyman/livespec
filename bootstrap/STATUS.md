# Bootstrap status

**Current phase:** 5
**Current sub-step:** Phase 5 §"v034 transition — replay-hook activation + drain authorization (v034 D3 / D5 / D6)" — step 2 complete (cycles 173-183 authored `red_green_replay.py` + paired test); step 3 about to land via `chore!:` activation commit (lefthook commit-msg stage wires `01-red-green-replay`; v033 `red_output_in_commit.py` + paired test deleted; bootstrap recipe extended with `.git/hooks/commit-msg` install + notes-refspec idempotent add; thinned aggregate stays — v034 D6 baseline-grandfathered mechanism deferred per bootstrap/decisions.md 2026-05-02T06:00:00Z); anti-cheat reflog inspection (originally cycle 184) deferred per cycle-183 commit body. Next: v034 D7 drain. The drain resolves the four unbound aggregate targets (`check-pbt-coverage-pure-modules`, `check-newtype-domain-primitives`, `check-schema-dataclass-pairing`, `check-complexity`) plus config-tier cleanup of `check-lint` and `check-format`; ~11-15 commits with `feat:`/`fix:` subjects following the v034 D2 trailer schema (the new replay hook gates each commit via lefthook commit-msg stage). Each drain cycle rejoins its now-passing target to the thinned aggregate in the same commit. Then v034 D5c quality-comparison report. Then Phase 5 exit gates (5a/5b/5c). Then v034 D8 branch protection activation as final Phase 5 sub-step.
**Last completed exit criterion:** phase 4
**Next action:** Drain cycle 1 — pick the first unbound canonical target (likely `check-pbt-coverage-pure-modules`: add `@hypothesis.given` PBT decorators to the three pure-module test modules per the style doc's "PBT coverage on pure modules" rule). First drain cycle is also the first commit that exercises the new v034 D2 trailer schema in production: subject `feat: ...` (or `fix: ...`); Red→Green amend pattern; `commit-msg` lefthook stage runs the replay hook which writes `TDD-Red-*` trailers into the commit message on the Red commit and `TDD-Green-*` trailers on the Green amend. Before the first drain cycle, the user must run `just bootstrap` once to install the new `.git/hooks/commit-msg` wrapper and add the notes refspec to local git config; the activation commit only updates the recipe text, not the .git/ tree (per "NEVER update the git config" rule).

**v034 scope (eight decisions):** D1 Conventional Commits + semantic-release adoption; D2 TDD-Red/Green trailer schema; D3 replay-based enforcement contract via new `dev-tooling/checks/red_green_replay.py`; D4 `refs/notes/commits` as advisory operational cache; D5 plan-text + dev-tooling enumeration housekeeping; D6 baseline mechanism via `phase-5-deferred-violations.toml` (replaces v033 thinned aggregate); D7 Phase 5 §"Aggregate-restoration drain" sub-section; D8 branch protection on master with deferred end-of-Phase-5 activation.

**Triggered by three concurrent issues:** (1) broken pushes to master because `just check` aggregate is thinned during the v033 D5b drain; CI matrix runs the full canonical-target list while the local hook runs only the thinned aggregate; (2) v033 D4's `## Red output` rule is honor-system content (cannot mechanically prove temporal Red→Green order); (3) PROPOSAL.md §"Versioning" describes spec versioning but does not describe livespec-the-software's version cadence — the Phase 10 v1.0.0 tag goal needs a machine-parseable commit-format path.

**Pre-v034 cycle history preserved as-is:** the v033 D5b second-redo cycles 1-172 used the v033 discipline (`## Red output` honor system; `phase-N: cycle N — ...` commit prefix). They are grandfathered: commitlint will exclude pre-v034-codification ancestor SHAs, and the replay-hook will skip commits without `feat:`/`fix:` subjects.

Open issues: zero unresolved.
**Last updated:** 2026-05-02T06:30:00Z
**Last commit:** activation commit pending (chore!: activate v034 replay hook + remove v033 hook; lefthook commit-msg stage wires `01-red-green-replay`; bootstrap recipe installs .git/hooks/commit-msg + idempotent-adds notes refspec; v033 `red_output_in_commit.py` + paired test deleted; thinned aggregate retained — v034 D6 baseline-grandfathered mechanism deferred per bootstrap/decisions.md 2026-05-02T06:00:00Z; full `just check` aggregate green at 23/23)
