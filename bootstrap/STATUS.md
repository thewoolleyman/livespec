# Bootstrap status

**Current phase:** 5
**Current sub-step:** Phase 5 §"Aggregate-restoration drain (v034 D7)" — drain cycle 2 complete (`check-newtype-domain-primitives` bound; `livespec/types.py` authored with 4 NewTypes; 4 raw-`str` fields migrated in `schemas/dataclasses/finding.py`, `seed_input.py`, `revise_input.py`; `dev-tooling/checks/newtype_domain_primitives.py` taught to peel `X | None` shapes per PEP 604, with 3 new paired test cases). Drain cycle 3 (`check-schema-dataclass-pairing`) is next per the plan §"Aggregate-restoration drain" estimate (~5 cycles, one per schema/dataclass/validator triple).
**Last completed exit criterion:** phase 4
**Next action:** Drain cycle 3 — `check-schema-dataclass-pairing`. Audit which schema/dataclass/validator triples currently fail the check; fix one per cycle under the v034 D2-D3 Red→Green amend pattern (the per-cycle-workflow, with `feat:`/`fix:` Conventional Commit type since the work is genuine behavior change). The replay hook gates each at the `commit-msg` stage; pre-commit's `check-pre-commit` aggregate skips test execution at Red commit per v036 D1.

**Drain rhythm (per 2026-05-02T09:55:00Z `bootstrap/decisions.md` entry):** drain cycles use the Conventional Commit type that honestly describes the work — `test:` for test-coverage strengthening (cycle 1), `chore:` for residual config-tier cleanup, `feat:`/`fix:` for genuine behavior change with full v034 D2 trailer schema via the replay hook + Red→Green amend pattern (cycle 2 onward; per v036 D1 the Red commit's failing test is allowed by `just check-pre-commit`'s Red-mode classifier), `refactor:` for pure restructuring. Same commit binds the now-passing target back into the aggregate.

**v034 scope (eight decisions):** D1 Conventional Commits + semantic-release adoption; D2 TDD-Red/Green trailer schema; D3 replay-based enforcement contract via `dev-tooling/checks/red_green_replay.py`; D4 `refs/notes/commits` as advisory operational cache; D5 plan-text + dev-tooling enumeration housekeeping; D6 baseline mechanism via `phase-5-deferred-violations.toml` (deferred indefinitely per v035 D1); D7 Phase 5 §"Aggregate-restoration drain" sub-section (in progress); D8 branch protection on master with deferred end-of-Phase-5 activation.

**Triggered by three concurrent issues:** (1) broken pushes to master because `just check` aggregate is thinned during the v033 D5b drain; CI matrix runs the full canonical-target list while the local hook runs only the thinned aggregate; (2) v033 D4's `## Red output` rule is honor-system content (cannot mechanically prove temporal Red→Green order); (3) PROPOSAL.md §"Versioning" describes spec versioning but does not describe livespec-the-software's version cadence — the Phase 10 v1.0.0 tag goal needs a machine-parseable commit-format path.

**Pre-v034 cycle history preserved as-is:** the v033 D5b second-redo cycles 1-172 used the v033 discipline (`## Red output` honor system; `phase-N: cycle N — ...` commit prefix). They are grandfathered: commitlint will exclude pre-v034-codification ancestor SHAs, and the replay-hook will skip commits without `feat:`/`fix:` subjects.

Open issues: zero unresolved.
**Last updated:** 2026-05-02T18:15:00Z
**Last commit:** drain-cycle-2 pending (feat: livespec/types.py NewType primitives + dataclass migrations; Red commit was 6f0c52e with 5 Red trailers; Green amend stages livespec/types.py + 3 dataclass migrations + check-fix to peel `X | None` + 3 paired test additions + justfile aggregate-rebind; aggregate 24 → 25 post-amend). v036 codification at sha 1754534; v036 implementation at sha 70b0752.
