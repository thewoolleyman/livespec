# Bootstrap status

**Current phase:** 4
**Current sub-step:** 3
**Last completed exit criterion:** phase 3
**Next action:** Phase 4 sub-step 3 — author the next batch of dev-tooling enforcement scripts. Candidates (simpler-first ordering): `main_guard.py` (bans `if __name__ == "__main__":` in livespec/**); `all_declared.py` (verifies `__all__: list[str]` declared in every module under livespec/, bin/, dev-tooling/); `no_inheritance.py` (direct-parent allowlist per v013 M5); `no_write_direct.py` (bans sys.stdout/stderr.write outside 3 designated surfaces); `private_calls.py` (bans cross-class _-prefixed access); `global_writes.py` (no module-level mutable state writes from functions; with the explicit exemption list per style doc lines 1497-1506). Each script + paired test in `tests/dev-tooling/checks/test_<name>.py` covering pass + fail cases. Sub-step 2 closed: authored `dev-tooling/checks/wrapper_shape.py` (verifies the 6-statement bin/*.py shape per v016 P5 + style doc lines 1664-1668) + `tests/dev-tooling/checks/test_wrapper_shape.py` (6 tests passing, both pass and fail cases covered). Two pyproject.toml fixes landed: tests/**.py per-file-ignore for S101 (idiomatic pytest assert); removed invalid `--icdiff` from pytest addopts (pytest-icdiff auto-loads via entry_points and produces nicer diffs by default). Established the dev-tooling/checks/<name>.py pattern: standalone script with `__future__` annotations + ast walk + logging-based diagnostic emission + main() returning int + `if __name__ == "__main__":` entry guard (banned in livespec/** but not in dev-tooling/).
**Last updated:** 2026-04-26T21:32:08Z
**Last commit:** 3b810f3
