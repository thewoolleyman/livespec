# Bootstrap status

**Current phase:** 5
**Current sub-step:** Phase 5 §"Aggregate-restoration drain (v034 D7)" — drain cycle 1 (`check-pbt-coverage-pure-modules`) complete; drain cycle 2 (`check-newtype-domain-primitives`) is next. Cycle 1 added `@given(...)` PBT decorators to the three pure-layer test modules (`tests/livespec/parse/test_jsonc.py`, `tests/livespec/validate/test_revise_input.py`, `tests/livespec/validate/test_seed_input.py`) and bound `check-pbt-coverage-pure-modules` into the `just check` aggregate's `targets=(...)` list at `justfile:75-99`. Aggregate now 24/24 (was 23/23). Drain remaining: 3 content-gap targets (`check-newtype-domain-primitives`, `check-schema-dataclass-pairing`, `check-complexity`) plus config-tier cleanup of `check-lint` and `check-format`. Then v034 D5c quality-comparison report. Then Phase 5 exit gates (5a/5b/5c). Then v034 D8 branch protection activation as final Phase 5 sub-step.
**Last completed exit criterion:** phase 4
**Next action:** Drain cycle 2 — `check-newtype-domain-primitives`. Create `.claude-plugin/scripts/livespec/types.py` with NewType-based domain primitives per PROPOSAL.md §"`livespec/types.py`" + migrate the four raw-`str` fields the check identifies. This is genuine behavior change → `feat:` Conventional Commit subject under v034 D2 Red→Green amend pattern (replay hook at `commit-msg` stage gates it). The Green amend rejoins `check-newtype-domain-primitives` into the aggregate at `justfile:75-99`. Estimated 2-3 cycles per the plan §"Aggregate-restoration drain" budget (one cycle per migrated field-set, or one cycle per NewType if the check enumerates them separately).

**Drain rhythm (per 2026-05-02T09:55:00Z `bootstrap/decisions.md` entry):** drain cycles use the Conventional Commit type that honestly describes the work — `test:` for test-coverage strengthening (cycle 1), `chore:` for residual config-tier cleanup, `feat:`/`fix:` for genuine behavior change (full v034 D2 trailer schema via the replay hook), `refactor:` for pure restructuring. Same commit binds the now-passing target back into the aggregate. The earlier STATUS framing of "~11-15 commits with `feat:`/`fix:` subjects" was overstated; actual rhythm is mixed by target. PROPOSAL.md §"Required-vs-skipped by commit type" (lines 3617-3624) + plan type-table (lines 2431-2437) already enumerate the no-trailer types — this is a clarification of usage, not a spec change.

**v034 scope (eight decisions):** D1 Conventional Commits + semantic-release adoption; D2 TDD-Red/Green trailer schema; D3 replay-based enforcement contract via `dev-tooling/checks/red_green_replay.py`; D4 `refs/notes/commits` as advisory operational cache; D5 plan-text + dev-tooling enumeration housekeeping; D6 baseline mechanism via `phase-5-deferred-violations.toml` (deferred indefinitely per v035 D1; thinned-aggregate-grows-as-passing pattern is the canonical mechanism); D7 Phase 5 §"Aggregate-restoration drain" sub-section (in progress); D8 branch protection on master with deferred end-of-Phase-5 activation.

**Triggered by three concurrent issues:** (1) broken pushes to master because `just check` aggregate is thinned during the v033 D5b drain; CI matrix runs the full canonical-target list while the local hook runs only the thinned aggregate; (2) v033 D4's `## Red output` rule is honor-system content (cannot mechanically prove temporal Red→Green order); (3) PROPOSAL.md §"Versioning" describes spec versioning but does not describe livespec-the-software's version cadence — the Phase 10 v1.0.0 tag goal needs a machine-parseable commit-format path.

**Pre-v034 cycle history preserved as-is:** the v033 D5b second-redo cycles 1-172 used the v033 discipline (`## Red output` honor system; `phase-N: cycle N — ...` commit prefix). They are grandfathered: commitlint will exclude pre-v034-codification ancestor SHAs, and the replay-hook will skip commits without `feat:`/`fix:` subjects.

Open issues: zero unresolved.
**Last updated:** 2026-05-02T09:55:00Z
**Last commit:** drain-cycle-1 pending (test: PBT decorators on parse/jsonc + validate/{revise,seed}_input + bind check-pbt-coverage-pure-modules to aggregate; aggregate 23 → 24)
