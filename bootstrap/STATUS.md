# Bootstrap status

**Current phase:** 4
**Current sub-step:** 11
**Last completed exit criterion:** phase 3
**Next action:** Phase 4 sub-step 11 — author next batch. Candidates: `match_keyword_only.py` (every `match` class-pattern destructuring a livespec-authored class uses `kwd_patterns` not positional `patterns`; third-party-types like `returns`'s `Success`/`Failure` permitted positionally per style doc lines 1352-1378 + 1811); `assert_never_exhaustiveness.py` (`assert_never` called in match-statement `case _:` exhaustiveness checks); `newtype_domain_primitives.py` (field-name → NewType mapping enforced for canonical domain primitives per livespec/types.py table); `public_api_result_typed.py` (every public function per `__all__` returns Result/IOResult, except supervisors and `build_parser` per style doc line 1806). Sub-step 10 closed: authored `dev-tooling/checks/keyword_only_args.py` + 18 paired tests AND a repo-wide refactor of 33 violations across 11 categories — `_unreachable(value)` → kw-only in 7 supervisors + parse/jsonc.py; 5 single-arg helpers refactored; `get_logger(name)` → kw-only + 7 call-site updates; 11 validator closures refactored to kw-only via new `TypedValidator[T]` generic Protocol in `livespec/types.py` (replacing `Callable[[dict], Result[T, E]]` aliases that pyright-strict rejected for kw-only assignment); 7 dev-tooling/checks helpers refactored. Style doc updated with two new exemptions: ROP-chain DSL callbacks (name positional to `.bind`/`.map`/`.alt`/`.lash`/`.apply`/`.bind_result`/`.bind_ioresult`) AND Protocol method definitions (Logger Protocol mirrors structlog's positional API). `no_inheritance.py` updated to recognize parametric Protocol bases (`Protocol[T]` as Subscript-of-Name). 95 dev-tooling tests passing. Phase 4 progress: 11 of ~22 enforcement scripts done.
**Last updated:** 2026-04-27T23:30:00Z
**Last commit:** a3e7f28
