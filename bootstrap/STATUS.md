# Bootstrap status

**Current phase:** 4
**Current sub-step:** 15
**Last completed exit criterion:** phase 3
**Next action:** Phase 4 sub-step 15 (next enforcement script per plan line 1466-1496 enumeration). Sub-step 14 closed in three commits: **14a** (`8d8874e`'s grandparent `26ae1b9`) refactored `commands/seed.py` from module-level `_xxx` functions into `@rop_pipeline class SeedPipeline` with single public `run(*, argv)` method; auto-capture cluster (~30 LLOC) extracted to sibling `commands/_seed_helpers.py` exporting `now_iso` and `write_seed_pair`; `_payload` initialized via `cast("SeedInput", None)` placeholder so chain steps read from `self` without per-method narrowing asserts; final 198 LLOC. **14b** (`f00e940`) mirrored the refactor for `commands/revise.py` → `@rop_pipeline class RevisePipeline` + sibling `commands/_revise_helpers.py` (constants, `compute_next_vnnn`, `spec_root_for_template`, `resolve_default_spec_target`, `render_revision`, `walk_writes`); final 173 LLOC. **14c** (`8d8874e`) authored `dev-tooling/checks/file_lloc.py` (tokenize.NEWLINE-token count, BUDGET=200, scoped to `.claude-plugin/scripts/livespec/**` + `.claude-plugin/scripts/bin/**` + `dev-tooling/**`) + 8 paired tests covering boundary, comment/blank-line exclusion, syntax-error handling, and real-repo pass; wired into `just check-complexity` per style doc canonical target list line 1876 (ruff C90/PLR + file_lloc.py shell-sequenced with rc accumulation), rather than authoring a standalone `check-file-lloc` target. Test count: 131 (up from 123, +8 from file_lloc). 14 of ~22 enforcement scripts done. Pre-existing PLR2004 / C901 / PLR0912 errors in keyword_only_args.py / match_keyword_only.py / rop_pipeline_shape.py predate this sub-step and are independent items to clear before Phase 4 exit.
**Last updated:** 2026-04-27T00:20:51Z
**Last commit:** 8d8874e
