# Bootstrap status

**Current phase:** 3
**Current sub-step:** 23
**Last completed exit criterion:** phase 2
**Next action:** Phase 3 sub-step 23 — exercise the throwaway-tmp_path exit-criterion test per plan line 1307-1344. Run `/livespec:seed` against a tmp_path fixture (with the seed dialogue answering "yes" to v020 Q2 and naming `livespec` and `minimal`) producing a valid `.livespec.jsonc`, the main spec tree with `history/v001/`, AND both template sub-spec trees under `<tmp>/SPECIFICATION/templates/{livespec,minimal}/` each with their own `history/v001/`. Then file a propose-change against the main tree via `/livespec:propose-change --spec-target <tmp>/SPECIFICATION` and revise it via `/livespec:revise --spec-target <tmp>/SPECIFICATION`. Then v020 Q3 sub-spec routing smoke cycle: second propose-change/revise targeting `<tmp>/SPECIFICATION/templates/livespec`. Confirm `<tmp>/SPECIFICATION/templates/livespec/history/v002/` materializes. Sub-step 22 closed: widened the 4 livespec-template prompts (seed.md, propose-change.md, critique.md, revise.md) to Phase 3 minimum-viable per v020 Q4. Each prompt now correctly references its target schema (seed_input.schema.json / proposal_findings.schema.json (shared by propose-change and critique) / revise_input.schema.json), specifies the v020 Q2 sub_specs[] dialogue branches for seed, the field-to-section mapping for propose-change, the critique-as-propose-change-delegation contract, and the per-proposal accept/modify/reject decision shape for revise. Fixed Phase-2 stub drift in propose-change.md (was referencing a non-existent `propose_change_input.schema.json`; now correctly uses `proposal_findings.schema.json`). Phase 7 replaces all 4 prompts via the dogfooded propose-change/revise loop against the livespec template's sub-spec.
**Last updated:** 2026-04-26T18:16:48Z
**Last commit:** 0551c9a
