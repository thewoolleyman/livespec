# Bootstrap status

**Current phase:** 4
**Current sub-step:** 10
**Last completed exit criterion:** phase 3
**Next action:** Phase 4 sub-step 10 — author `keyword_only_args.py`. Rule per style doc lines 1300-1350 + 1810: every `def` in livespec scope (`.claude-plugin/scripts/livespec/**`, `.claude-plugin/scripts/bin/**`, `dev-tooling/**`) MUST place a lone `*` as first parameter (after `self`/`cls` for methods) so every subsequent parameter is in `kwonlyargs`; every `@dataclass` MUST carry the strict-dataclass triple `frozen=True, kw_only=True, slots=True`. Exemptions: dunder methods with Python-mandated signatures (`__eq__`, `__hash__`, `__getitem__`, `__iter__`, `__next__`, etc.); `__init__` of Exception subclasses with single positional `msg`; `__post_init__`. WARN: ~25+ private helpers (e.g., `_orchestrate(namespace)`, `_unreachable(value)`, `_to_precondition(exc)`) currently lack the `*` separator — refactor scope expected. Sub-step 9 closed: authored `dev-tooling/checks/no_except_outside_io.py` (try/except clauses outside `livespec/io/**` restricted to supervisor catch-all in `main()` of `commands/*.py` + `doctor/run_static.py`) + 9 paired tests; refactored `parse/jsonc.py` (raw try/except → `@safe(exceptions=(json.JSONDecodeError,))` + `match` on Result, per style doc §"Error Handling Discipline" lines 545-560) and `doctor/static/proposed_change_topic_format.py` (dropped defensive `except OSError` in `_extract_topic`). 76 dev-tooling tests passing total. Phase 4 progress: 10 of ~22 enforcement scripts done.
**Last updated:** 2026-04-27T22:55:00Z
**Last commit:** b59730e
