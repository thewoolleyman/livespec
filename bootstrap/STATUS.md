# Bootstrap status

**Current phase:** 3
**Current sub-step:** 15
**Last completed exit criterion:** phase 2
**Next action:** Phase 3 sub-step 15 — author `livespec/commands/critique.py` (minimum-viable per v019 Q1): invokes `propose_change.py` internally with the `-critique` reserve-suffix appended (the simplest delegation shape; full reserve-suffix algorithm lives in Phase 7). Accepts `--spec-target <path>` and routes the delegation with the same target. Out-of-Phase-3 scope: full critique prompt-driven flow (LLM-side); critique-as-internal-delegation is the wrapper-level mechanic only. Sub-step 14 closed: authored `livespec/commands/propose_change.py` with full Phase 3 minimum-viable per v019 Q1 — `--findings-json <path> <topic> [--author <id>] [--spec-target <path>] [--project-root <path>]` CLI; reads + parses + schema-validates the findings JSON against `proposal_findings.schema.json` (exit 4 on validation failure); rejects non-canonical topics with exit 4 ("topic not canonical"); two-source author precedence (CLI → `LIVESPEC_AUTHOR_LLM` env → `unknown-llm` fallback); spec-target resolution (explicit flag OR upward `.livespec.jsonc` walk → derived `<project_root>/SPECIFICATION/` for livespec built-in or `<project_root>` for minimal); collision detection at `<spec-target>/proposed_changes/<topic>.md` (exit 3 PreconditionError); writes proposed-change file with YAML front-matter (topic, author, created_at) + one `## Proposal: <name>` section per finding with all four sub-headings (Target specification files, Summary, Motivation, Proposed Changes). ruff clean.
**Last updated:** 2026-04-26T16:15:17Z
**Last commit:** 997a64f
