# Bootstrap status

**Current phase:** 3
**Current sub-step:** 17
**Last completed exit criterion:** phase 2
**Next action:** Phase 3 sub-step 17 — author `livespec/doctor/run_static.py` orchestrator per PROPOSAL §"Static-phase structure" + v014 N3 bootstrap lenience + v018 Q1 per-tree iteration (applicability dispatch finalized in v021 Q1). Enumerates `(spec_root, template_name)` pairs at startup (main tree first with template-name sentinel `"main"`; then each sub-spec under `<main-spec-root>/templates/<sub-name>/` with template-name set to the sub-spec directory name); per-pair builds a per-tree `DoctorContext` and runs the applicable check subset per the orchestrator-owned applicability table (template_exists / template_files_present main-only; gherkin_blank_line_format only for livespec template; everything else uniform). Sub-step 16 closed: authored `livespec/commands/revise.py` with full Phase 3 minimum-viable per v019 Q1 — `--revise-json` payload schema-validated against revise_input.schema.json; spec-target resolution via `.livespec.jsonc` (or explicit flag); next-vNNN discovery by scanning history/ for v\d+ entries; author_human via io.git.get_git_user with GIT_USER_UNKNOWN fallback; author_llm via CLI/env/payload/unknown-llm precedence; per-decision processing (accept/modify/reject) writes revision files, moves proposed-change files into history/vNNN/proposed_changes/, applies resulting_files updates to working spec; on any accept/modify, snapshots .md files from spec_target root into history/vNNN/. Added `remove_file` primitive to io/fs.py for the move semantics (read+write+remove composition; same-filesystem assumption acceptable for Phase 3 minimum-viable). Internal `_RevisionContext` dataclass bundles the 5 shared fields per decision so `_walk_decisions`/`_process_one_decision` stay under PLR0913's 6-arg cap. Out-of-Phase-3 scope: per-version README (Phase 7), v014 N6 collision-suffix handling (Phase 7), cross-filesystem move fallback (Phase 7), full LLM-driven decision flow (Phase 7).
**Last updated:** 2026-04-26T16:55:03Z
**Last commit:** 06bb47c
