# Bootstrap status

**Current phase:** 4
**Current sub-step:** 14
**Last completed exit criterion:** phase 3
**Next action:** Phase 4 sub-step 14 (split into 14a/14b/14c). **14a:** refactor `commands/seed.py` (currently 225 LLOC) into `@rop_pipeline class SeedPipeline:` with single public `run(self, *, argv)` method; convert top-level `_xxx` functions to private methods (using `self`); update chain composition to `.bind(self._orchestrate)` etc. (the keyword_only_args ROP-DSL exemption now recognizes `Attribute` args, so bound-method callbacks work); keep `main()` + `build_parser()` module-level; extract the auto-capture cluster (seed.py lines 599-680, ~30 LLOC) into a sibling helper module `commands/_seed_helpers.py` exporting public functions (`write_seed_auto_capture`, `write_seed_files`); SeedPipeline calls them via positional ROP callbacks (`.bind(write_seed_auto_capture)` — bare-name form, name imported from helper module). Verify all 123 existing tests + the new shape checks still pass; confirm seed.py < 200 LLOC. **14b:** same refactor for `commands/revise.py` (currently 233 LLOC). **14c:** author `dev-tooling/checks/file_lloc.py` (file ≤ 200 logical lines per style doc lines 1457-1472, tokenize NEWLINE-token count) + paired test; once both supervisors are under budget, `test_main_passes_against_real_repo` will pass and the check lands. Halt-and-revise to v029 completed earlier in this sub-step's setup: PROPOSAL.md `dev-tooling/checks/` directory listing realigned to include 5 missing checks (`rop_pipeline_shape.py`, `heading_coverage.py`, `vendor_manifest.py`, `check_tools.py`, `check_mutation.py`) and `keyword_only_args.py` annotation now enumerates the full strict-dataclass triple; plan-level edits paired (version basis preamble, v029 decisions block, Phase 0 byte-identity ref, frozen-status header, execution-prompt authoritative-version line, Phase 4 enforcement-script enumeration carrying rop_pipeline_shape.py); originating open-issues entry resolved; snapshot at `history/v029/`. Sub-step 13 closed earlier: authored `rop_pipeline` decorator (livespec/types.py — runtime no-op + AST marker) + `dev-tooling/checks/rop_pipeline_shape.py` (every `@rop_pipeline` class has exactly one public method; dunders not counted) + 9 paired tests; extended `keyword_only_args.py` ROP-DSL exemption to recognize `Attribute` args like `.bind(self._method)`; style doc gains §"ROP pipeline shape" section + canonical-target-list entry; justfile updated; decorator chosen over base class since the `no_inheritance` allowlist is intentionally tiny. 123 dev-tooling tests passing. Phase 4 progress: 14 of ~22 enforcement scripts done (rop_pipeline_shape counted; file_lloc remains pending until 14c).
**Last updated:** 2026-04-28T00:50:00Z
**Last commit:** fba51ea
