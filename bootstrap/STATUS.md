# Bootstrap status

**Current phase:** 4
**Current sub-step:** 14
**Last completed exit criterion:** phase 3
**Next action:** Phase 4 sub-step 14 — refactor `commands/seed.py` (225 LLOC) and `commands/revise.py` (233 LLOC) into `@rop_pipeline class SeedPipeline:` / `@rop_pipeline class RevisePipeline:` per the new ROP-pipeline-shape rule (single public method per pipeline class), then author `file_lloc.py` (file ≤ 200 LLOC) + paired test once both supervisors land under budget. Some chain-step methods may need to spill into helper modules under public names (e.g., `commands/_seed_writers.py` exporting `write_main_files`, `materialize_history`, etc.) since moving them into the class as private methods doesn't reduce file LLOC. Sub-step 13 closed: authored `rop_pipeline` decorator (livespec/types.py — runtime no-op + AST marker), authored `dev-tooling/checks/rop_pipeline_shape.py` (every `@rop_pipeline` class has exactly one public method + dunders not counted) + 9 paired tests; extended `keyword_only_args.py` ROP-DSL exemption to recognize `Attribute` args like `.bind(self._method)` (so chain steps inside pipeline classes work as bound-method callbacks); style doc gains §"ROP pipeline shape" section + canonical-target-list entry; justfile updated. Decorator chosen over base class since `no_inheritance` allowlist is intentionally tiny. 123 dev-tooling tests passing. Phase 4 progress: 14 of ~22 enforcement scripts done. PENDING: PROPOSAL.md's `dev-tooling/checks/` directory listing (lines 3496-3520) does NOT yet include `rop_pipeline_shape.py`, and was already missing `heading_coverage.py`, `vendor_manifest.py`, `check_tools.py` before this session. Decision needed: batch-update PROPOSAL.md via halt-and-revise (vNNN bump) or treat the directory listing as indicative.
**Last updated:** 2026-04-28T00:15:00Z
**Last commit:** be95c8a
