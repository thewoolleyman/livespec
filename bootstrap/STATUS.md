# Bootstrap status

**Current phase:** 3
**Current sub-step:** 13
**Last completed exit criterion:** phase 2
**Next action:** Phase 3 sub-step 13 — author `livespec/commands/seed.py` per PROPOSAL §"`seed`" (lines ~1865-2050): full implementation of the seed wrapper. `--seed-json <path>` (required) intake; pre-seed `.livespec.jsonc` lookup with v016 P2 + v017 Q5/Q6 three-branch handling (absent → bootstrap defaults; present + matching template → continue; present + mismatching → exit 3; malformed → exit 3); idempotency refusal when template-declared target files already exist; v001 history materialization; auto-capture of `seed.md` proposed-change + `seed-revision.md`. Per v018 Q1 + v020 Q2, the wrapper materializes sub-spec trees under `SPECIFICATION/templates/<name>/` atomically alongside the main tree. Sub-step 12 closed: authored `livespec/commands/resolve_template.py` with full implementation per PROPOSAL §"Template resolution contract" (post-v028 D1 corrected formula): build_parser() pure factory + run() railway + main() supervisor with bug-catcher + match dispatch on the final IOResult. Bundle-root via `Path(__file__).resolve().parents[3]`. Default flow walks upward for `.livespec.jsonc`, parses + schema-validates against livespec_config, resolves the template field. Pre-seed flow uses --template directly. Built-in names map to `<bundle-root>/specification-templates/<name>/`; user paths resolve relative to --project-root. Validation surface: directory exists + contains template.json (deeper validation deferred to template-exists doctor check). Side benefit: extended `Logger` Protocol in io/structlog_facade.py with `exception()` method for supervisor bug-catchers. v028 PROPOSAL revision landed (commit 9d79238) before sub-step 12 work began.
**Last updated:** 2026-04-26T16:08:42Z
**Last commit:** 9d79238
