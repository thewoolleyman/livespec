# Bootstrap status

**Current phase:** 7
**Current sub-step:** Phase 7 sub-step 5 (revise widening) IN-PROGRESS. Sub-substeps 5.a `9eca318` (filed propose-change against v038-canonical Statement B contract) ✓ and 5.b `6646aa4` (revise accepts; spec edits applied; v011 cut at SPECIFICATION/history/v011/) ✓ both COMPLETE. Next: 5.c — Red→Green impl widening of `livespec/commands/revise.py` per v033 D5b + v034 D2-D3 contracts. Sub-step 4 (critique widening) COMPLETE per prior STATUS — sub-substeps 4.a `659fa95`, 4.b `30c9631`, 4.c `de1c110`.
**Last completed exit criterion:** phase 6
**Next action:** Begin sub-step 5.c — Red→Green impl widening of `livespec/commands/revise.py`. Per the v011 spec, the widening covers: (a) full 5-key revision-file front-matter (proposal, decision, revised_at, author_human, author_llm — currently the Phase-3-minimum `_compose_revision_body` emits only proposal + decision); (b) per-decision-type sections (`## Modifications` for modify; `## Resulting Changes` for accept/modify; `## Rejection Notes` for reject — currently only `## Decision and Rationale` is emitted); (c) working-spec snapshot into history/vNNN/ on every successful revise per Statement B (currently absent from `_write_and_move_per_decision`); (d) explicit PreconditionError on empty proposed_changes/. Plan as multiple focused Red→Green cycles per the 4.c pattern (single bundled cycle is also acceptable if the test surface is naturally cohesive; executor's choice based on code shape).

**v038 halt-and-revise summary (2026-05-04T03:43:15Z → 2026-05-04T06:55:00Z):**

Phase 7 sub-step 5.a's cascading-impact scan caught a PROPOSAL.md §"Versioning" internal contradiction between Statement A (line 1734-1736 iff: cut "when, and only when, accepts or modifies") and Statement B (line 1739-1745: all-reject MUST still cut). The two cannot both hold mechanically. Surfaced when the draft propose-change Proposal 3 ("revise lifecycle and responsibility separation") needed to codify the version-cut rule. Original open-issues entry: `2026-05-04T03:43:15Z — phase 7 — blocking — halt-and-revise-brainstorming`; resolved via v038 D1 + D2 codification.

User selected Statement B as authoritative per PROPOSAL line 1745's stated intent ("preserves the audit trail for every proposal that ever reached `revise`"). v038 D1 PROPOSAL edits at two loci: §"Versioning" line 1734-1738 iff softened to "on every successful revise invocation (i.e., processes at least one proposal)"; §"`revise`" line 2483-2484 rewritten to match (cut on every successful revise; byte-identical-to-prior spec files when no decision is `accept`/`modify`). Line 1739-1745 case-handler retained verbatim. v038 D2 plan-text + housekeeping: Version basis preamble v038 decision block; Phase 0 step 1 byte-identity → `history/v038/`; Phase 0 step 2 frozen-status → "Frozen at v038" (no-op convention since v024); Execution-prompt block authoritative-version → v038. No follow-up implementation commit required — the Phase-3 minimum-viable `livespec/commands/revise.py` already implements Statement B's "always cut on non-empty processing" semantic via `_process_decisions` always invoking `_next_history_version_dir` regardless of decision content; Phase 7 sub-step 5.c will widen the impl under v038's now-canonical contract. v038 codification commit at `1280ae6` (chore!: codify v038 — Statement B authoritative on the version-cut rule); push to origin/phase-7-widen-sub-commands confirmed via fetch + log.

**Sub-step 4.c implementation summary (2026-05-04, preserved from prior STATUS):**

Single Red→Green cycle landed in two commits (Red `ad6ed2e` superseded by Green amend at `de1c110`):

- **Red commit** added 4 new tests + env-isolation `monkeypatch.delenv("LIVESPEC_AUTHOR_LLM", raising=False)` to two existing fallback tests in `tests/livespec/commands/test_critique.py`. Three of four new tests fail at HEAD (env, payload, truncate); the CLI-wins test passes pre-widening as a regression guard.
- **Green amend** widened `livespec/commands/critique.py`:
  - `_resolve_author` signature widened from `(*, namespace)` to `(*, namespace, payload, env_lookup)` matching `propose_change._resolve_author`'s signature exactly. Body identical to propose_change's reference impl (DRY-violation accepted between two private module-internal helpers; cross-module calls into private helpers would violate the module-private boundary per `commands/CLAUDE.md`).
  - `main()` refactored to thread the validated `ProposalFindings` payload + `os.environ.get` through to `_resolve_author`. Pattern-match restructured: a new `_validate_with_namespace(*, namespace) -> IOResult[tuple[Namespace, ProposalFindings], LivespecError]` helper pairs the validated payload with the namespace through `.map(...)`; the supervisor's `match unwrapped:` Success arm extracts both via tuple-unpack.
  - `_pattern_match_io_result` helper removed (was a compatibility shim that returned only the exit code; the widened Success arm needs richer behavior so the match is inlined in `main()`).
  - New `_build_delegated_argv(*, namespace, resolved_author)` helper composes the propose_change argv list per spec.md §"`critique` internal delegation": `--reserve-suffix=-critique` (note: `=` syntax single-arg form to avoid argparse interpreting `-critique` as a flag attempt), `--author=<resolved>` (so propose_change's own front-matter author resolution short-circuits at step 1), and the un-slugged resolved-author stem as the trailing positional topic-hint. `--spec-target` and `--project-root` forwarded when present.

**v034 D2-D3 amend hiccup (codified):** the first impl draft passed `--reserve-suffix` and `-critique` as two separate argv tokens — argparse rejected this because `-critique` looks like a flag attempt, lifting to `UsageError` (exit 2) in propose_change's parser. Fix: use the `--reserve-suffix=-critique` single-arg `=` syntax. Lesson: when forwarding flag values that begin with `-`, always use the `=` form. Captured here so future delegation-shape work doesn't re-derive this.

**Phase-7 architectural follow-up logged in `decisions.md` 2026-05-03T10:50:00Z:** `heading_coverage.py` treats `## Proposal:` headings under `proposed_changes/` as spec content; PROPOSAL.md §"Coverage registry" lines 3795-3798 narrowly define `spec_root` as tree-root only. Pre-existing PROPOSAL drift accepted at Phase 6; deferred to Phase 7 propose-change cycle or Phase 8 deferred-items entry. Not blocking sub-step 5.

**Sub-step 3.c implementation summary (preserved from prior session):**
- Cycle 1 — topic canonicalization (v015 O3): added `_canonical_alnum_run_strip(*, text)` shared primitive + `_canonicalize_topic(*, hint, reserve_suffix=None)`.
- Cycle 2 — reserve-suffix (v016 P3 / v017 Q1): widened `_canonicalize_topic` with `reserve_suffix` keyword.
- Cycle 3 — author resolution + YAML front-matter: added `_resolve_author(*, namespace, payload, env_lookup)` implementing 4-step precedence in propose_change.py.
- Cycle 4 — slug transformation (v014 N5): structural no-op (decisions.md 2026-05-04T02:10:00Z).
- Cycle 5 — collision disambiguation (v014 N6): added `_resolve_target_path(*, proposed_changes_dir, canonical_topic, existing_filenames)`.

**v034 scope (eight decisions):** D1 Conventional Commits + semantic-release adoption ✓; D2 TDD-Red/Green trailer schema ✓; D3 replay-based enforcement contract via `dev-tooling/checks/red_green_replay.py` ✓; D4 `refs/notes/commits` as advisory operational cache ✓; D5 plan-text + dev-tooling enumeration housekeeping ✓; D6 baseline-grandfathered-violations TOML mechanism (deferred indefinitely per v035 D1); D7 Phase 5 §"Aggregate-restoration drain" ✓; D8 branch protection on master ✓.

**Pre-v034 cycle history preserved as-is:** v033 D5b second-redo cycles 1-172 used the v033 discipline (`## Red output` honor system; `phase-N: cycle N — ...` commit prefix). Grandfathered: commitlint excludes pre-v034-codification ancestor SHAs.

**Last updated:** 2026-05-04T07:12:00Z
**Last commit:** `6646aa4` on phase-7-widen-sub-commands (sub-step 5.b: `chore: phase-7 sub-step 5.b — revise accepts revise widening`). Sub-step 5 commit chain (oldest→newest): `1280ae6` (v038 codification, pushed) → `9eca318` (5.a, unpushed) → `6646aa4` (5.b, unpushed). Sub-step 4 commit chain: `659fa95` (4.a) → `30c9631` (4.b) → `344b91f` (interim STATUS) → `ad6ed2e` (4.c Red, superseded) → `de1c110` (4.c Green amend) → `6a807d1` (4.c STATUS) → `1280ae6` (v038 codification). Sub-step 3.c commits at `deb48f5` (cycle 1), `8004263` (cycle 2), `be2beef` (cycle 3), `9c42ccd` (cycle 4 decision-only), `4112ded` (cycle 5). Sub-step 3.a `c8a6be2`, 3.b `92468a6`. Branch is 45 commits ahead of origin/master; origin/phase-7-widen-sub-commands at `1280ae6` (5.a + 5.b unpushed).
