# Bootstrap status

**Current phase:** 4
**Current sub-step:** 1
**Last completed exit criterion:** phase 3
**Next action:** Begin Phase 4 — Developer tooling enforcement scripts. Author every enforcement check under `dev-tooling/checks/` per the canonical just-target list (per plan line 1411-1445). Scripts include: `file_lloc.py`, `private_calls.py`, `global_writes.py`, `supervisor_discipline.py`, `no_raise_outside_io.py` (raise-site only per v017 Q3), `no_except_outside_io.py`, `public_api_result_typed.py` (per v012 L9), `main_guard.py`, `wrapper_shape.py` (permits optional blank line per v016 P5), `schema_dataclass_pairing.py` (three-way per v013 M6), `keyword_only_args.py` (also verifies frozen+kw_only+slots dataclass triple), `match_keyword_only.py`, `no_inheritance.py` (direct-parent allowlist per v013 M5), `assert_never_exhaustiveness.py`, `newtype_domain_primitives.py`, `all_declared.py`, `no_write_direct.py`, `pbt_coverage_pure_modules.py`, `claude_md_coverage.py`, `no_direct_tool_invocation.py`, `no_todo_registry.py` (release-gate only), `heading_coverage.py` (per-tree per v018 Q1), `vendor_manifest.py`. Each script standalone; each has paired `tests/dev-tooling/checks/test_<name>.py`. Each `dev-tooling/` directory carries a CLAUDE.md. Phase 4 exit criterion: `just check` passes against current code base; every check invokable + non-trivial. Phase 3 closed: full python implementation surface (errors + types + context + 6 io modules + parse + 30 schema/dataclass/validator files + 5 commands + 8 doctor static checks + registry + orchestrator) + 6 SKILL.md prose + 4 livespec-template prompts + tmp_path round-trip exit-criterion test all working end-to-end (including v020 Q3 sub-spec routing smoke). v028 PROPOSAL revision (bundle-root formula correction) landed mid-phase. Two implementation bugfixes surfaced during Phase 3 work and got fixed: TRY003 per-file-ignore (sub-step 4), revise.py path-resolution (sub-step 23). Two plan-fixes (Phase 2 sub-step 6 + Phase 3 sub-step 23) corrected exit-criterion sentences to drop dev-tooling-backed checks deferred to Phase 5.
**Last updated:** 2026-04-26T20:54:04Z
**Last commit:** a8fe693
