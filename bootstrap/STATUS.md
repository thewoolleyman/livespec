# Bootstrap status

**Current phase:** 3
**Current sub-step:** 14
**Last completed exit criterion:** phase 2
**Next action:** Phase 3 sub-step 14 — author `livespec/commands/propose_change.py` (minimum-viable per v019 Q1): parses an inline-authored proposed-change file's front-matter against `proposed_change_front_matter.schema.json`, writes it to `<spec-target>/proposed_changes/<topic>.md`, surfaces collisions as exit-3 PreconditionError. `--spec-target <path>` flag (default: main spec root, resolved via `.livespec.jsonc` upward walk). Out-of-Phase-3 scope: topic canonicalization (v015 O3), reserve-suffix canonicalization (v016 P3 / v017 Q1), unified author precedence beyond simplest two-source rule, collision disambiguation prompts, single-canonicalization invariant routing — Phase 7 widens. Sub-step 13 closed: authored `livespec/commands/seed.py` with full Phase 3 minimum-viable implementation per PROPOSAL §"`seed`": parse + schema-validate `--seed-json` payload (exit 4 on validation failure), three-branch `.livespec.jsonc` state-check (absent → continue; present + matching template → continue; present + mismatching → exit 3 PreconditionError), idempotency target-existence check, atomic file-shaping (`.livespec.jsonc` write + main files + sub-spec files + history/v001/ materialization for main + each sub-spec + auto-capture seed.md/seed-revision.md). `_derive_spec_root` hardcoded for v1 built-ins (livespec → `SPECIFICATION/`; minimal → repo root) — Phase 7 widens to read template.json's `spec_root` field. Idempotency stops at first existing file rather than gathering full list — Phase 7 widens. Per-version README omitted in this minimum-viable scope; v001 audit trail covered through seed.md/seed-revision.md. ruff clean.
**Last updated:** 2026-04-26T16:12:18Z
**Last commit:** 34e143a
