# tests/livespec/spec_governance/

Tests for the `livespec.spec_governance` package. Keep them
focused on pure registry/config/effective-policy behavior, text
preservation helpers, and journal validation; wrapper plumbing
belongs under `tests/bin/` and command-supervisor behavior under
`tests/livespec/commands/`.
