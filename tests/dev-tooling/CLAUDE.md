# tests/dev-tooling/

Tests for the enforcement scripts under `dev-tooling/`. Mirrors
`<repo-root>/dev-tooling/` one-to-one. Each script has a paired
`tests/dev-tooling/checks/test_<name>.py` that covers BOTH pass
and fail cases.
