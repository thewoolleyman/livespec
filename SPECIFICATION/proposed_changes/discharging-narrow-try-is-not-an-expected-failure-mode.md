---
topic: discharging-narrow-try-is-not-an-expected-failure-mode
author: claude-opus-5
created_at: 2026-08-02T18:03:45Z
---

## Proposal: A narrow discharging `try` is not an expected failure mode — correct member 1's clause (b)

### Target specification files

- non-functional-requirements.md

### Summary

§"ROP composition" member 1 clause (b) disqualifies a function from the no-expected-failure-mode set when its body contains ANY `try` statement, and clause (d) propagates that disqualification to every caller. A `try` whose handlers name SPECIFIC exception types and every one of which ENDS IN A `return` does not leave an expected failure anywhere to flow: the failure originates inside the statement and is answered inside it, converted to a defined value for that input class. Clause (b) counts it anyway, so the rule DEMANDS a `Result` whose failure track is exactly the UNINHABITED one member 1's own rationale exists to prevent. This proposal CORRECTS clause (b) to exclude a DISCHARGING NARROW `try`, defined syntactically in four limbs. It is a correction to the CRITERION in the same shape as v184's failability correction — NOT a fifth member of the exemption set above, which remains EXHAUSTIVE — because a function whose only `try` discharges narrowly never had an expected failure mode to exempt. The NARROWNESS limb is mandatory and costs nothing: measured across the fleet, ZERO of the functions this correction relieves are relieved by a broad handler.

### Motivation

**THE PRINCIPLE FIRST, AND THE COUNT LAST — the count is a convenience argument and this proposal does not rest on it.** Every function the correction relieves is a PARSE-OR-CLASSIFY function that catches a parse error and returns a defined value for that input class: `parse_json`, `parse_float`, `parse_iso_datetime`, `loads_json_optional`, `classify`, `check_tmux_segment`, `extract_created_worktree_paths`, `run_adopter_rows`. Not one is a filesystem, process or network call. **A relaxing change that relieved a mixed bag would be the declared-empty escape wearing a new name; this one relieves a single coherent semantic shape** — and it relieves `livespec` and `livespec-overseer` NOT AT ALL, though between them they hold 188 of the fleet's 411 convictions. A coherent class rather than a scattering is what separates a WIDENING of what the criterion recognises from a SOFTENING of the criterion, and that is the distinction on which v181 turned.

**THE MECHANICAL RULE REPRODUCES A HUMAN RULING IT WAS NEVER TOLD ABOUT, which is the only argument here about whether the rule is RIGHT rather than about what it DOES.** `livespec-dev-tooling` has three remaining convictions, and all three were ruled BY HAND — earlier, independently, and each for its own stated reasons — as NOT conversions. The correction reproduces TWO of those three verdicts (`extract_created_worktree_paths`, `run_adopter_rows`) without being told, and REFUSES the third (`cross_member_consumption`). **The refusal is the more informative half.** `cross_member_consumption` is disqualified through `_parsed`, whose handler is narrow (`except SyntaxError`) but RECORDS the failure into an out-parameter and continues the loop rather than returning. That is the in-band CENSUS design — "here is the graph, and here is what could not be measured" — which is a real design question about the value's shape and NOT a question about whether a failure escapes. A correction that relieved it too would be a blanket, and the four limbs are what stop it being one.

**MEASURED. THE CONTROL, THE IDENTITY CONTROL, AND THE HAZARD ARM.** Re-derived on `livespec-dev-tooling` master `7ffec46` on 2026-08-02 with the shipped criterion — `_find_offenders` over `resolve_check_universe()`, the `_`-prefixed FILE skip dropped — and the probe feeds rewritten source ONLY to the member-1 computation while conviction is computed from the ORIGINAL sources:

```
CONTROL      shipped criterion            universe 171   offenders 3   (the three known names)
IDENTITY     identity rewrite             universe 171   offenders 3   moves NOTHING
NARROW       four-limb discharge          universe 171   offenders 1   relieves 2
LOOSE        limb (ii) removed            universe 171   offenders 1   relieves the SAME 2
```

**The identity control is not optional and it caught a contaminated instrument.** An earlier probe of this same question patched `Path.read_text` globally, so the conviction pass ALSO read `ast.unparse`d text; it reported this repo moving 3 → 21, an INCREASE a relaxing change cannot produce. `ast.unparse` strips comments and parts of this analysis read them. The design above — rewritten text to the member-1 computation only — measures 3 → 3 under an identity rewrite, which is what makes the 3 → 1 above quotable.

**THE FLEET FIGURE, with its as-of stated because a part and a total from different days do not add.** Measured 2026-08-02 across all nine members at their then-current masters, one criterion and one denominator:

| repo | before | after | relieved |
|---|---:|---:|---|
| `livespec-orchestrator-beads-fabro` | 172 | 169 | `parse_json`, `parse_float`, `parse_iso_datetime` |
| `livespec-dev-tooling` | 3 | 1 | `extract_created_worktree_paths`, `run_adopter_rows` |
| `livespec-orchestrator-git-jsonl` | 18 | 17 | `loads_json_optional` |
| `livespec-driver-codex` | 2 | 1 | `check_tmux_segment` |
| `livespec-driver-claude` | 1 | 0 | `classify` |
| `livespec-overseer`, `livespec`, `livespec-runtime` | — | unchanged | — |
| **fleet** | **411** | **403** | **8, or 2%** |

Only the `livespec-dev-tooling` row was re-derived today; the other members' masters have since moved by 6 to 8 commits each, so their figures are quoted as of that measurement rather than as of now.

**THE CORRECTION SPLITS EVENLY ACROSS ITS TWO HALVES, AND A RULE WRITTEN AS ONLY ONE OF THEM RELIEVES HALF THE POPULATION.** Four of the eight (`parse_json`, `parse_float`, `parse_iso_datetime`, `loads_json_optional`) are the functions that CONTAIN the `try` and are relieved by clause (b) locally. The other four (`extract_created_worktree_paths`, `run_adopter_rows`, `classify`, `check_tmux_segment`) contain no `try` at all and are relieved because clause (d) stops propagating a callee's. Both halves follow from one change to clause (b), which is why this proposal states the correction there and states the propagation consequence explicitly rather than legislating it separately.

**AND THE NARROWNESS LIMB WAS TESTED RATHER THAN ASSUMED, because the hazard is real.** The obvious way to write this rule — "a `try/except` that returns a defined value does not disqualify" — would exempt, by its own terms, exactly the population ruff `BLE` exists to convict: a blind `except Exception: return None` IS "a defined value for that input class" under a loose reading, and §"ROP composition" binds `BLE` precisely to keep that shape out. Measured on every member with limb (ii) removed: **ZERO functions anywhere in the fleet are relieved only by a broad handler.** All eight reliefs name specific exception types; every handler on the relieving path is `except json.JSONDecodeError`, `except ValueError` or `except SyntaxError`, each read rather than grepped. **The hazard is not hypothetical: broad discharging constructs DO exist across the fleet** — `livespec-dev-tooling` alone carries three that a loose predicate would admit — they simply sit today where they relieve nothing. **So the narrow limb costs nothing measured and forecloses the BLE-swallowing failure mode; writing it loose would buy nothing and spend the guardrail.**

**ONE RESIDUAL IS NAMED RATHER THAN LEFT TO BE DISCOVERED, and it is a clause (e) SPELLING gap this correction UNMASKS rather than creates.** Clause (e) refuses the `X | None` shape because no AST can tell a failure-`None` from an absence-`None`. It reads the ANNOTATION, so an `Any` return defeats it: `livespec-orchestrator-git-jsonl`'s `loads_json_optional(*, text: str) -> Any` returns `None` on a caught `json.JSONDecodeError`, which IS the hand-rolled failure track clause (e) exists to refuse, and clause (e) does not see it. Today clause (b) disqualifies that function anyway, so the gap is MASKED; under this correction it becomes visible as one of the eight. **That is a defect in clause (e)'s spelling, not a reason to write this correction loose or to withhold it** — the same function annotated `dict | None` is still refused. Deliberately NOT proposed here: any change to clause (e). Its exposure cannot be honestly quoted before it is mechanized (this section's own rule), and it deserves its own measurement rather than a rider on this one.

**Deliberately NOT proposed:** any change to the four-member exemption set, to clause (a), (c), (d) or (e), to the conservative-in-the-disqualifying-direction rule, to the store-no-claim obligation, or to any part of the boundary-handler cardinality rules in §"Supervisor discipline". This correction is confined to what clause (b) COUNTS.

This proposal adds no new `## ` heading — it amends one clause and adds bolded blocks inside the existing §"ROP composition" section — so it carries no `tests/heading-coverage.json` co-edit obligation. It states no user-observable livespec behavior, so it carries no `scenarios.md` obligation either, on the same footing as v179, v183, v184 and v185, which amended this section without one.

### Proposed Changes

In `non-functional-requirements.md` §"ROP composition", in the **MEMBER 1 — MECHANICALLY DECIDED, RECOMPUTED EVERY RUN** clause list, REPLACE clause (b):

- (b) no `try` statement;

with:

- (b) no `try` statement OTHER THAN a DISCHARGING NARROW `try`, as defined below;

and INSERT the following four blocks immediately after the paragraph beginning *"The analysis MUST be CONSERVATIVE IN THE DISQUALIFYING DIRECTION"* and before the block beginning *"**CLAUSE (d) IS LOAD-BEARING AND IS NOT A REFINEMENT.**"*:

**WHAT CLAUSE (b) COUNTS — A DISCHARGING NARROW `try` IS NOT AN EXPECTED FAILURE MODE.** A `try` statement is DISCHARGING NARROW when a purely syntactic analysis of the statement shows ALL of: (i) it has AT LEAST ONE handler and NO `finally` block; (ii) EVERY handler names SPECIFIC exception types — a bare `except:`, an `except Exception` and an `except BaseException` DISQUALIFY unconditionally, whatever the handler body does; (iii) EVERY handler's body ENDS IN A `return`, so the caught failure is CONVERTED into a defined value for that input class rather than recorded in band and carried onward; and (iv) NO `raise` appears anywhere inside the statement, its handlers included. A `try` meeting ALL FOUR MUST NOT disqualify under clause (b). A `try` meeting FEWER than all four MUST disqualify under clause (b) exactly as before, and the analysis MUST resolve any doubt about a limb as DISQUALIFYING, per the conservative direction stated above.

**THIS IS A CORRECTION TO THE CRITERION, NOT A FIFTH MEMBER OF THE EXEMPTION SET ABOVE, WHICH REMAINS EXHAUSTIVE.** Nothing is exempted from the railway here. An expected failure that originates inside such a statement is ANSWERED inside it and reaches no caller, so the function never had an expected failure mode to flow, and a `Result` over it would carry precisely the UNINHABITED failure track member 1's rationale exists to prevent — the dead unwraps that hide the live ones. The correction is the same shape as the failability correction in this section's **What counts as an I/O boundary** block: both remove from the criterion a syntactic proxy that convicts a place no failure can escape from. **AND IT DOES NOT DEPEND ON THE HANDLERS BEING COMPLETE, which is the objection this limb set is built to answer.** An exception the handlers do NOT name still escapes the statement — and under this section's own error-handling routing that escape is a BUG, which MUST propagate as a raised exception and is by definition not an expected failure mode. The named types are the expected failures and they are discharged; whatever is unnamed is bug-class and is correctly outside the rule. A handler broad enough to catch both is what limb (ii) refuses. It is NOT an entitlement any consumer can declare into: like the rest of member 1 it stores NO claim and is recomputed from the code on EVERY run, so widening a handler to `except Exception`, deleting a handler's `return`, adding a `finally`, or adding a `raise` re-arms the rule at that commit. **A conforming implementation MUST decide all four limbs mechanically and MUST NOT accept any declaration asserting that a `try` discharges.**

**CLAUSE (d) FOLLOWS, AND THE PROPAGATION HALF MUST NOT BE READ AS ACCIDENTAL.** Clause (d) propagates (a)–(d) over the call graph, so a callee whose ONLY disqualifier was a discharging narrow `try` no longer disqualifies its callers. That consequence is INTENDED and is the larger half of this correction's effect: measured across the governed fleet, half the functions it relieves contain no `try` at all and are relieved solely because a callee's stopped propagating. A conforming implementation MUST NOT implement the correction as a caller-side special case; it applies at the `try` statement, and (d)'s fixpoint carries it.

**WHAT THIS CORRECTION DOES NOT DO, stated because the relieving reading is the dangerous one.** FIRST, it does NOT reach clause (e): a function whose own `return` annotation is `X | None` stays disqualified however narrowly it discharges, and member 2 remains the only route for a legitimate absence. SECOND, it does NOT relieve a handler that RECORDS a failure and continues — an in-band census that reports what could not be measured is a real design question about the VALUE's shape, and limb (iii) refuses it deliberately rather than by omission. THIRD, limb (ii) is LOAD-BEARING and MUST NOT be relaxed on the argument that a broad handler which returns is equally total: a broad handler cannot tell the expected failure it means to catch from the bug it does not, so what it returns is a defined value for an UNKNOWN input class, and §"Linter rule set" binds ruff `BLE` precisely to keep that shape out. Measured before ratification: ZERO functions in the governed fleet are relieved by a broad handler, so the narrow limb costs nothing measured while foreclosing the swallowing failure mode. FOURTH, relief is NOT remediation: a function leaving the offender list under this correction may still carry an unrelated defect — one relieved function's `Result`-returning private helper has an uninhabited failure track and a dead `Failure` arm — and a COUNT THAT MOVES WITHOUT FIXING is the same mistake as a check that REPORTS WITHOUT SCANNING, seen from the other end.

⚠️ **AND ONE RESIDUAL IS UNGUARDED, STATED HERE RATHER THAN LEFT TO BE DISCOVERED.** Clause (e) reads the return ANNOTATION, so an `Any` (or absent) annotation defeats it: a function that returns a bare `None` from a discharging narrow handler under such an annotation is relieved here even though its `None` models a FAILURE, which is the hand-rolled failure track clause (e) exists to refuse. One function in the governed fleet is in that position today; clause (b) masks it at present, and this correction makes it visible. This is a defect in clause (e)'s SPELLING rather than in the correction above — the same function annotated `X | None` is still refused — and it MUST be addressed by its own amendment, measured after mechanization rather than estimated before it.

**THIS RULE'S DESIGN RECORD** (per `spec.md` §"Intent preservation and design-record authority"). The four limbs, the fleet measurement, the loose-versus-narrow control, and the identity-rewrite control that invalidated an earlier probe are recorded in repo `thewoolleyman/livespec-dev-tooling`, `plan/rop-railway-enforcement/handoff.md`, under ledger epic `livespec-dev-tooling-8o8e`. The deciding reasoning, preserved there: clause (b) was a SYNTACTIC PROXY for "a place an expected failure can originate and escape", and a `try` that names its exceptions and returns from every handler is the one shape where the proxy and the property it stands for come apart — the failure originates and is discharged in the same statement. The relieved population was required to be a single coherent semantic class before the correction was filed, and it is: every one is a parse-or-classify function returning a defined value for an unparseable input, and the mechanical rule independently reproduces two dispositions this fleet had already ruled by hand while refusing a third that a blanket reading would have taken.
